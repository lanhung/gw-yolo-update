# GW-YOLO research status — 2026-07-21

## Executive status

The repository and remote experiment system are operational and reproducible, but the paper-level
scientific endpoint is not yet reached. The strongest completed result is a negative scaling result:
increasing unique analytic waveforms from 2,000 to 10,000 while holding the same 76 detector-noise
GPS blocks does not materially improve candidate ranking. This rules out an immediate 25k/50k
waveform-only expansion and promotes independent GPS/run/glitch diversity, multi-Q content and
aligned detector-set modelling instead.

No O4b/GWTC-5 strain has been opened for model, threshold, calibration or OOD selection. Current
catalog/image metrics are not reported as search recall. A paper claim remains gated on continuous
background FAR/IFAR, injection efficiency and `<VT>` at a frozen threshold.

## Reproducible execution snapshot — 09:21 UTC

The active branch is `agent/locked-evaluation-access` at commit `510540b`; the same commit is
published as GitHub `main`. The complete CPU test suite and Ruff checks pass. Remote execution uses only new
`/root/GW-YOLO-v2-*` directories; `/root/GW-YOLO`, its weights and its historical runs remain
untouched. At the snapshot, the root filesystem has about 14 GB free, the external data volume has
about 35 GB free and no unowned GPU process is being disturbed.

Progress is reported on two separate axes because a single percentage would hide the dominant
scientific gap:

| Axis | Completed | Current interpretation |
|---|---:|---|
| Reproducible engineering and lock infrastructure | about 85% | stable CLI, group-safe manifests, hashes, atomic writes, locked-test gate, PE adapters and resumable remote queues exist; artifact-registry/paper-table automation remains |
| Paper evidence gates | 3/11 | leakage-safe baseline, physical 2k/5k/10k scale decision and O4a lock protocol are complete; real-glitch network training, OOD, FAR/`<VT>`, five-seed promotion, PE and locked transfer remain |

The aligned Gravity Spy train acquisition has five of seven shards finalized: 1,082/1,345 planned
rows (80.4%) are fully accounted, of which 859 pass and 223 are rejected for non-finite companion
strain. Thus the current usable materialized fraction is 859/1,345 (63.9%), not 1,082/1,345.
Completed reports cover 74/91 source files (81.3%).
Shard 5 is active; ten of its fourteen source files are fully verified. Counting those
verified-but-not-yet-materialized files, 84/91 train source files (92.3%) have passed full-file
verification. Shard 6 is a final one-row, three-source component.
The 306-row validation bank is frozen but intentionally waits for train-cache release.

A row-level audit shows that up to 200 of the 223 current rejects have a valid event IFO plus a
second valid companion in their planned three-IFO context; 23 two-IFO rows cannot meet the network
minimum after their bad companion is removed. New code now represents this as an explicit runtime
detector-set downgrade rather than rejecting the whole row. These 200 remain a recovery estimate
until a fresh materialization verifies the other two IFOs; they are not included in the 859 count.

Several wrappers had retained `SIGSTOP` after an earlier resource-priority intervention. Their exact
commands and PIDs were rechecked and only the six project wrappers that should have been waiting
normally were resumed. The intentionally paused 800-pair morphology job remains paused until the
formal AMPLFI run releases the single GPU. No process was terminated.

Recent controls close four reproducibility gaps:

- AMPLFI checkpoints are indexed from trusted Lightning artifacts and selected only by validation
  metrics; incomplete schedules, `last.ckpt` and rows with populated test metrics are ineligible.
- A frozen score-blind sparse time-slide schedule is derived from background GPS/availability and
  the target zero-count exposure. The current 40-pair engineering range reaches only 98,800 s versus
  726,640,593 s required for a 0.1/year 90% upper bound, so `target_attained=false` is preserved.
- Streamed background merge reports zero-lag live time and detector time by run, split and detector
  subset; the full 800-pair plan is queued for this accounting path.
- Completed Gravity Spy shards now take a hash-verified idempotent fast path. A restart validates
  run identity, report, manifest, every numeric sample, partial inventory and counts before reuse;
  it never silently reacquires evicted source strain or accepts a corrupted sample.

## Completed evidence

- The leakage-safe legacy segmentation baseline, numeric data factory, manifest/hash provenance,
  atomic writes, validation-only checkpoint selection and CPU test suite are in place.
- Five 10k analytic-data seeds completed. The validation-selected seed has mean IoU 0.88810; the
  five-seed mean is 0.88087 with sample standard deviation 0.00678. This is a representation result,
  not a continuous-search result.
- A frozen 3,000-injection validation corpus and a group-safe 10,000-injection training corpus were
  materialized in verified O4a noise. The nested 2k/5k/10k candidate experiment retains every
  proposal and never counts repeated candidates as physical examples.
- The STFT candidate-pair scaling gate failed 3/4 predeclared checks. Fixed-update overall padded
  top-1 is 0.3322/0.3238/0.3322 at 2k/5k/10k and peak p90 remains about 4.34 s. The machine diagnosis
  is `waveform_scale_plateau_with_fixed_gps_support`; 25k/50k waveform-only scaling is blocked.
- A same-parent, same-900-update three-resolution STFT control was precommitted and completed. It
  changes overall top-1 from 0.3322 to 0.3339, SNR 8--15 top-1 from 0.6849 to 0.7123 and peak p90
  from 4.4769 s to 4.4028 s. Only the BNS/NSBH non-regression checks pass; all three improvement
  checks fail, so the arm is not allowed to scale to 5k.
- Gravity Spy single-IFO numeric materialization contains 1,505 train and 326 validation rows with
  split-safe physical IDs. A 93-task human weak-mask audit plan is frozen.
- Physical chirp+real-glitch mixtures contain 300 train and 100 validation rows and pass the joint
  waveform, injection, glitch, GPS and mixture cross-split audit.
- A one-seed single-IFO overlap fine-tune completed but failed the predeclared clean-retention gate:
  no epoch reached the required 0.95 clean chirp-IoU retention, and glitch-mask IoU remained zero.
  It is retained as a negative control and is not promoted to five seeds.
- A Blip-held-out OOD baseline used a threshold fitted only on 307 known validation examples. It
  held known abstention to 15/307 (4.89%) but abstained on 0/17 held-out Blips; unknown false
  acceptance is 17/17, with Wilson 95% interval 81.57%--100%, and diagnostic AUROC is 0.5294.
  Closed-set embedding confidence is therefore not an acceptable OOD policy.
- A second family was frozen by sample count before its scores were opened: Tomte contributes 27
  unknown rows over 10 GPS blocks, with 26 additional known rows in the same held-block evaluation.
  A precommitted supervised-contrastive/logit-energy arm improves diagnostic AUROC to 0.7735 and
  abstains on 5/27 Tomtes, but still falsely accepts 22/27 and falsely abstains on 3/26 known rows.
  It is directional evidence only and also fails promotion.
- The O4a endpoint now has an executable two-stage lock: corpus freezing records the immutable
  test manifest and predeclared access-log path without opening it; the one-time opening gate
  verifies the manifest hash, rejects physical-group overlap against every declared training,
  selection and calibration manifest, hashes the frozen model/config/threshold/OOD artifacts and
  atomically creates an exclusive access receipt. A second opening fails before test access.
- The independent-GPS scale test now has a causal paired arm and a frozen promotion gate. The same
  injection/waveform population is remapped onto GPS blocks disjoint from both the baseline and
  shared validation data, then compared under both fixed-update and fixed-epoch budgets with paired
  seeds and BBH/BNS/NSBH non-regression checks. This separates GPS/domain diversity from merely
  drawing another random waveform population of the same size.
- Morphology-only background shards can now be calibrated as a validation single-IFO trigger rate
  using true detector-time exposure. The report explicitly forbids a network FAR claim; it is an
  early rejection diagnostic while empirical 10 ms timing/coincidence remains unresolved.
- The publication PE adapter now rejects a nominal DINGO/AMPLFI comparison unless both backends use
  the identical injection set and byte-identical clean/contaminated/mask-conditioned inputs. It
  verifies input, contamination, mask-model and mask-policy hashes and enforces common prior,
  waveform, detector, calibration, hardware and end-to-end latency definitions across backends.
- A separate PE backend lock now pins DINGO v0.9.8 and AMPLFI v0.6.0 sources and refuses publication
  readiness until distinct CUDA environments, exact installed versions, domain-compatible model
  files and model metadata are all present and hash-frozen. The shared initial comparison is BBH on
  byte-identical H1/L1 4,096 Hz, 16-second source artifacts. The current committed model hashes are deliberately
  unresolved, so this is a truthful failing gate rather than a claim that package installation or
  external inference is complete.
- A standardized PE model-sidecar freezer now rejects test-selected checkpoints and binds each
  model to its validation selection report, training configuration/data manifest, common analysis
  prior, native conditioning, waveform conventions and inference-parameter set. The readiness audit
  re-hashes those artifacts and requires the DINGO/AMPLFI common prior, analysis waveform and output
  parameters to match before either result can enter a paper comparison.
- The official O4a DINGO release at `https://zenodo.org/records/21221337` is now the preferred
  model source rather than the older O3 archive. Its `SEOBNRv5PHM_v5PHM_H1L1` model is a 4.8 GB,
  high-mass H1/L1 precessing network. The downloaded settings checksum matches the published MD5
  `3dbd2867949d671fbfe8b65dde098654`; metadata records DINGO 0.5.8, epoch 225, ten million waveform
  samples, a 16-second/4,096 Hz window, 20--1,024 Hz analysis band and fourteen native parameters.
  Loading this model under the pinned 0.9.8 runtime is a compatibility test, not an assumption; a
  native 0.5.8 environment remains the fallback if serialized-model compatibility fails.
- AMPLFI v0.6's bundled CBC configuration instead uses H1/L1, 3 seconds at 2,048 Hz,
  `IMRPhenomPv2` and eight native output parameters. No reusable official checkpoint has yet been
  identified; its paper Zenodo record is queued for inspection after the core GWOSC downloads.
  Fairness therefore uses one 16-second/4,096 Hz source artifact, backend-native derived tensors,
  and an explicit common-parameter mapping rather than pretending native tensor shapes or output
  spaces are identical.
- Official O4a DINGO acquisition is now a locked CLI operation rather than an ad hoc download. The
  manifest freezes exact Zenodo sizes and MD5 values for the 4,823,584,930-byte posterior model and
  1,070,177,454-byte time model. Downloads are opt-in, resumable, disk-reserve guarded and atomically
  promoted only after integrity verification; the resulting report adds local SHA-256 identities.
- The AMPLFI training bridge now exports only contiguous, DQ-approved H1/L1 runs from the frozen
  GPS-block split into native HDF5 files. It verifies source hashes, performs anti-aliased 4,096 to
  2,048 Hz resampling and refuses cross-split blocks. A custom `GroupSafeFlowDataset` replaces
  AMPLFI's default file-order validation selection and re-audits block identities/live time at model
  setup. The publication config retains the full official 800-epoch schedule.
- A backend-neutral common BBH prior and an AMPLFI-native projection are now machine-compared across
  fourteen source and nuisance parameters. The gate verifies distribution families, bounds,
  detector set and native sample/window settings, preventing a nominal paired comparison under
  different marginalized spin, sky or distance priors.

## Active acquisition

- The aligned Gravity Spy training plan covers 1,345/1,505 rows, 448 network GPS blocks and 91
  official source files. Detector subsets are H1L1=572, H1L1V1=721, H1V1=51 and L1V1=1. Seven
  bounded shards are being downloaded, full-file verified, DQ checked, converted to aligned numeric
  arrays and evicted only after output verification.
- The independently frozen validation plan covers 306/326 rows, 123 network GPS blocks and 95
  source files. Its subsets are H1L1=85, H1L1V1=189, H1V1=24 and L1V1=8. Seven source-connected
  shards have been frozen so no source file can cross shard boundaries; materialization is queued
  after the train cache is released.
- A second source-independent network bank is frozen after excluding every original glitch ID,
  network GPS block and official source file. The training selection has 1,653 glitches over 786
  GPS blocks and 127 source files; validation has 356 glitches over 155 GPS blocks and 126 source
  files. All selected source-connected components are kept intact. The bounded 100/30 per-family
  targets cannot be met for Paired Doves and Wandering Line (and validation Helix), so these are
  recorded as real rare-family deficits rather than hidden with rendered augmentation. Nine
  source-connected shards per split and a hash-verified recovered-base + independent-source union
  path are frozen for execution after the current recovery chain.
- A global plan audit found that the historical train/validation roles, although disjoint in
  glitch ID and 64-second GPS block, share 17 official 4,096-second source files; the independent
  roles share 18, and the four-way union has 59 cross-role source files. These historical roles
  will not be used directly for network training. After materialization, all 3,660 unique glitches
  are frozen again with score-blind `source_component_balanced_v1`, grouping both source-file
  connectivity and GPS blocks before assigning train/validation. The union has 161 indivisible
  components over 380 source files and 1,512 GPS blocks; every one of the 21 families occurs in at
  least five independent components, so source-disjoint family coverage is feasible. A mandatory
  corpus audit rejects any glitch, GPS block, source URL or numeric sample hash crossing the final
  roles before overlap generation.
- The first fresh O4a background shard has 1,393 eight-second windows over 45 GPS blocks, split by
  `hash_threshold_v1` into 668 train, 301 validation and 424 test windows with zero block overlap.
- Nine additional non-overlapping four-pair shards (parent-plan pair indices 4--39) are queued after
  aligned Gravity Spy acquisition. A global 40-pair stable-hash merge and exposure audit will run
  automatically afterward; passing depends on post-DQ validation exposure, not raw file count.

## Continuous-background gap

The fresh validation split has only 301 windows over 10 GPS blocks. Three hundred declared
positive-lag shifts yield 361,200 s = 0.01145 yr of observed background exposure. With zero events,
the 90% FAR upper limit would still be 201.17/yr. This is only 0.0497% of the exposure required for
a 0.1/yr claim. Even the optimistic all-unordered-pairs bound requires 13,479 valid dual-IFO
eight-second windows for a ten-year IFAR-scale claim. The current corpus is therefore an engineering
calibration shard, not a publishable FAR measurement.

## Ordered remaining work

1. Finish and audit aligned H1/L1/V1 Gravity Spy train/validation materialization.
2. Build network-aware chirp+glitch overlaps and train a shared per-IFO encoder with detector
   validity masks, dual instance masks and a clean non-inferiority gate.
3. Replace the failed OOD baseline with a validation-calibrated energy/distance or contrastive arm,
   then evaluate held-family and observing-run transfer without retuning.
4. Expand independent O4a GPS-block background to at least the frozen IFAR exposure target, score
   every retained candidate, fit the threshold on validation only, and freeze it.
5. Run injection efficiency, FAR/IFAR, sensitive distance and `<VT>` with paired bootstrap intervals
   and detector/glitch/SNR/population strata.
6. Promote only a gate-passing architecture to at least five seeds.
7. Compare paired raw, contaminated and mask-conditioned inputs with DINGO/AMPLFI-compatible PE
   using identical priors and waveform assumptions. Report coverage, bias, width, sky area, sample
   efficiency and latency; do not compare PE systems through detection mAP.
8. Freeze code/config/data/model/threshold/OOD hashes, then perform the one-time locked O4b/GWTC-5
   evaluation and prepare the PRD/JCAP/ApJS manuscript package.

The separate 5,000-injection O4a endpoint currently being materialized is a development-promotion
endpoint, not O4b/GWTC-5. It will remain unopened until the data-axis comparison, model checkpoint,
validation threshold and OOD policy are all frozen and the one-time opening receipt can be written.

## Schedule estimate

Assuming the remote GPU remains available and GWOSC downloads remain stable:

- remaining aligned train acquisition: roughly 3--10 hours at the observed variable GWOSC rate;
- aligned validation acquisition and final diversity audits: a further 8--30 hours, so 1--2 days
  end to end with a 3-day conservative allowance;
- network-aware overlap baseline and OOD replacement: 3--7 days;
- background expansion plus executable FAR/IFAR/`<VT>` benchmark: 1--3 weeks;
- five-seed promoted model and deglitch ablations: 1--2 additional weeks;
- paired DINGO/AMPLFI inference and calibration study: 2--4 additional weeks;
- locked evaluation, robustness tables and manuscript-quality reruns: 2--4 additional weeks.

The earliest credible end-to-end evidence package is therefore about 6--8 weeks; a conservative
paper-ready estimate is 8--12 weeks. These are execution estimates, not a promise that a positive
effect will pass the frozen gates. A negative gate result triggers a bounded redesign and may extend
the schedule. Public O4b/GWTC-5 strain availability is an external dependency for the final locked
claim.
