# GW-YOLO research status — 2026-07-21

## Executive status

The repository and remote experiment system are operational and reproducible, but the paper-level
scientific endpoint is not yet reached. The strongest completed result is a negative scaling result:
increasing unique analytic waveforms from 2,000 to 10,000 while holding the same 76 detector-noise
GPS blocks does not materially improve candidate ranking. This rules out an immediate 25k/50k
waveform-only expansion and promotes independent GPS/run/glitch diversity, multi-Q content and
aligned detector-set modelling instead.

No O4b/GWTC-5 strain has been opened for model, threshold, calibration or OOD selection. Current
catalog/image metrics are not reported as search recall. A paper claim remains gated on continuous
background FAR/IFAR, injection efficiency and `<VT>` at a frozen threshold.

## Reproducible execution snapshot — 09:47 UTC

The active branch is `agent/locked-evaluation-access` at commit `036937f`; the same commit is
published as GitHub `main`. The complete CPU test suite and Ruff checks pass. Remote execution uses only new
`/root/GW-YOLO-v2-*` directories; `/root/GW-YOLO`, its weights and its historical runs remain
untouched. At the snapshot, the root filesystem has about 14 GB free, the external data volume has
about 35 GB free and no unowned GPU process is being disturbed.

Progress is reported on two separate axes because a single percentage would hide the dominant
scientific gap:

| Axis | Completed | Current interpretation |
|---|---:|---|
| Reproducible engineering and lock infrastructure | about 85% | stable CLI, group-safe manifests, hashes, atomic writes, locked-test gate, PE adapters and resumable remote queues exist; artifact-registry/paper-table automation remains |
| Paper evidence gates | 3/11 | leakage-safe baseline, physical 2k/5k/10k scale decision and O4a lock protocol are complete; real-glitch network training, OOD, FAR/`<VT>`, five-seed promotion, PE and locked transfer remain |

The aligned Gravity Spy train acquisition has five of seven shards finalized: 1,082/1,345 planned
rows (80.4%) are fully accounted, of which 859 pass and 223 are rejected for non-finite companion
strain. Thus the current usable materialized fraction is 859/1,345 (63.9%), not 1,082/1,345.
Completed reports cover 74/91 source files (81.3%).
Shard 5 is active; twelve of its fourteen source files are fully verified. Counting those
verified-but-not-yet-materialized files, 86/91 train source files (94.5%) have passed full-file
verification. Shard 6 is a final one-row, three-source component.
The 306-row validation bank is frozen but intentionally waits for train-cache release.

A row-level audit shows that up to 200 of the 223 current rejects have a valid event IFO plus a
second valid companion in their planned three-IFO context; 23 two-IFO rows cannot meet the network
minimum after their bad companion is removed. New code now represents this as an explicit runtime
detector-set downgrade rather than rejecting the whole row. These 200 remain a recovery estimate
until a fresh materialization verifies the other two IFOs; they are not included in the 859 count.

Several wrappers had retained `SIGSTOP` after an earlier resource-priority intervention. Their exact
commands and PIDs were rechecked and only the six project wrappers that should have been waiting
normally were resumed. The intentionally paused 800-pair morphology job remains paused until the
formal AMPLFI run releases the single GPU. No process was terminated.

Recent controls close four reproducibility gaps:

- AMPLFI checkpoints are indexed from trusted Lightning artifacts and selected only by validation
  metrics; incomplete schedules, `last.ckpt` and rows with populated test metrics are ineligible.
- A frozen score-blind sparse time-slide schedule is derived from background GPS/availability and
  the target zero-count exposure. The current 40-pair engineering range reaches only 98,800 s versus
  726,640,593 s required for a 0.1/year 90% upper bound, so `target_attained=false` is preserved.
- Streamed background merge reports zero-lag live time and detector time by run, split and detector
  subset; the full 800-pair plan is queued for this accounting path.
- Completed Gravity Spy shards now take a hash-verified idempotent fast path. A restart validates
  run identity, report, manifest, every numeric sample, partial inventory and counts before reuse;
  it never silently reacquires evicted source strain or accepts a corrupted sample.

## Completed evidence

- The leakage-safe legacy segmentation baseline, numeric data factory, manifest/hash provenance,
  atomic writes, validation-only checkpoint selection and CPU test suite are in place.
- Five 10k analytic-data seeds completed. The validation-selected seed has mean IoU 0.88810; the
  five-seed mean is 0.88087 with sample standard deviation 0.00678. This is a representation result,
  not a continuous-search result.
- A frozen 3,000-injection validation corpus and a group-safe 10,000-injection training corpus were
  materialized in verified O4a noise. The nested 2k/5k/10k candidate experiment retains every
  proposal and never counts repeated candidates as physical examples.
- The STFT candidate-pair scaling gate failed 3/4 predeclared checks. Fixed-update overall padded
  top-1 is 0.3322/0.3238/0.3322 at 2k/5k/10k and peak p90 remains about 4.34 s. The machine diagnosis
  is `waveform_scale_plateau_with_fixed_gps_support`; 25k/50k waveform-only scaling is blocked.
- A same-parent, same-900-update three-resolution STFT control was precommitted and completed. It
  changes overall top-1 from 0.3322 to 0.3339, SNR 8--15 top-1 from 0.6849 to 0.7123 and peak p90
  from 4.4769 s to 4.4028 s. Only the BNS/NSBH non-regression checks pass; all three improvement
  checks fail, so the arm is not allowed to scale to 5k.
- Gravity Spy single-IFO numeric materialization contains 1,505 train and 326 validation rows with
  split-safe physical IDs. A 93-task human weak-mask audit plan is frozen.
- Physical chirp+real-glitch mixtures contain 300 train and 100 validation rows and pass the joint
  waveform, injection, glitch, GPS and mixture cross-split audit.
- A one-seed single-IFO overlap fine-tune completed but failed the predeclared clean-retention gate:
  no epoch reached the required 0.95 clean chirp-IoU retention, and glitch-mask IoU remained zero.
  It is retained as a negative control and is not promoted to five seeds.
- A Blip-held-out OOD baseline used a threshold fitted only on 307 known validation examples. It
  held known abstention to 15/307 (4.89%) but abstained on 0/17 held-out Blips; unknown false
  acceptance is 17/17, with Wilson 95% interval 81.57%--100%, and diagnostic AUROC is 0.5294.
  Closed-set embedding confidence is therefore not an acceptable OOD policy.
- A second family was frozen by sample count before its scores were opened: Tomte contributes 27
  unknown rows over 10 GPS blocks, with 26 additional known rows in the same held-block evaluation.
  A precommitted supervised-contrastive/logit-energy arm improves diagnostic AUROC to 0.7735 and
  abstains on 5/27 Tomtes, but still falsely accepts 22/27 and falsely abstains on 3/26 known rows.
  It is directional evidence only and also fails promotion.
- The O4a endpoint now has an executable two-stage lock: corpus freezing records the immutable
  test manifest and predeclared access-log path without opening it; the one-time opening gate
  verifies the manifest hash, rejects physical-group overlap against every declared training,
  selection and calibration manifest, hashes the frozen model/config/threshold/OOD artifacts and
  atomically creates an exclusive access receipt. A second opening fails before test access.
- The independent-GPS scale test now has a causal paired arm and a frozen promotion gate. The same
  injection/waveform population is remapped onto GPS blocks disjoint from both the baseline and
  shared validation data, then compared under both fixed-update and fixed-epoch budgets with paired
  seeds and BBH/BNS/NSBH non-regression checks. This separates GPS/domain diversity from merely
  drawing another random waveform population of the same size.
- Morphology-only background shards can now be calibrated as a validation single-IFO trigger rate
  using true detector-time exposure. The report explicitly forbids a network FAR claim; it is an
  early rejection diagnostic while empirical 10 ms timing/coincidence remains unresolved.
- The publication PE adapter now rejects a nominal DINGO/AMPLFI comparison unless both backends use
  the identical injection set and byte-identical clean/contaminated/mask-conditioned inputs. It
  verifies input, contamination, mask-model and mask-policy hashes and enforces common prior,
  waveform, detector, calibration, hardware and end-to-end latency definitions across backends.
- A separate PE backend lock now pins DINGO v0.9.8 and AMPLFI v0.6.0 sources and refuses publication
  readiness until distinct CUDA environments, exact installed versions, domain-compatible model
  files and model metadata are all present and hash-frozen. The shared initial comparison is BBH on
  byte-identical H1/L1 4,096 Hz, 16-second source artifacts. The current committed model hashes are deliberately
  unresolved, so this is a truthful failing gate rather than a claim that package installation or
  external inference is complete.
- A standardized PE model-sidecar freezer now rejects test-selected checkpoints and binds each
  model to its validation selection report, training configuration/data manifest, common analysis
  prior, native conditioning, waveform conventions and inference-parameter set. The readiness audit
  re-hashes those artifacts and requires the DINGO/AMPLFI common prior, analysis waveform and output
  parameters to match before either result can enter a paper comparison.
- The official O4a DINGO release at `https://zenodo.org/records/21221337` is now the preferred
  model source rather than the older O3 archive. Its `SEOBNRv5PHM_v5PHM_H1L1` model is a 4.8 GB,
  high-mass H1/L1 precessing network. The downloaded settings checksum matches the published MD5
  `3dbd2867949d671fbfe8b65dde098654`; metadata records DINGO 0.5.8, epoch 225, ten million waveform
  samples, a 16-second/4,096 Hz window, 20--1,024 Hz analysis band and fourteen native parameters.
  Loading this model under the pinned 0.9.8 runtime is a compatibility test, not an assumption; a
  native 0.5.8 environment remains the fallback if serialized-model compatibility fails.
- AMPLFI v0.6's bundled CBC configuration instead uses H1/L1, 3 seconds at 2,048 Hz,
  `IMRPhenomPv2` and eight native output parameters. No reusable official checkpoint has yet been
  identified; its paper Zenodo record is queued for inspection after the core GWOSC downloads.
  Fairness therefore uses one 16-second/4,096 Hz source artifact, backend-native derived tensors,
  and an explicit common-parameter mapping rather than pretending native tensor shapes or output
  spaces are identical.
- Official O4a DINGO acquisition is now a locked CLI operation rather than an ad hoc download. The
  manifest freezes exact Zenodo sizes and MD5 values for the 4,823,584,930-byte posterior model and
  1,070,177,454-byte time model. Downloads are opt-in, resumable, disk-reserve guarded and atomically
  promoted only after integrity verification; the resulting report adds local SHA-256 identities.
- The AMPLFI training bridge now exports only contiguous, DQ-approved H1/L1 runs from the frozen
  GPS-block split into native HDF5 files. It verifies source hashes, performs anti-aliased 4,096 to
  2,048 Hz resampling and refuses cross-split blocks. A custom `GroupSafeFlowDataset` replaces
  AMPLFI's default file-order validation selection and re-audits block identities/live time at model
  setup. The publication config retains the full official 800-epoch schedule.
- A backend-neutral common BBH prior and an AMPLFI-native projection are now machine-compared across
  fourteen source and nuisance parameters. The gate verifies distribution families, bounds,
  detector set and native sample/window settings, preventing a nominal paired comparison under
  different marginalized spin, sky or distance priors.

## Active acquisition

- The aligned Gravity Spy training plan covers 1,345/1,505 rows, 448 network GPS blocks and 91
  official source files. Detector subsets are H1L1=572, H1L1V1=721, H1V1=51 and L1V1=1. Seven
  bounded shards are being downloaded, full-file verified, DQ checked, converted to aligned numeric
  arrays and evicted only after output verification.
- The independently frozen validation plan covers 306/326 rows, 123 network GPS blocks and 95
  source files. Its subsets are H1L1=85, H1L1V1=189, H1V1=24 and L1V1=8. Seven source-connected
  shards have been frozen so no source file can cross shard boundaries; materialization is queued
  after the train cache is released.
- A second source-independent network bank is frozen after excluding every original glitch ID,
  network GPS block and official source file. The training selection has 1,653 glitches over 786
  GPS blocks and 127 source files; validation has 356 glitches over 155 GPS blocks and 126 source
  files. All selected source-connected components are kept intact. The bounded 100/30 per-family
  targets cannot be met for Paired Doves and Wandering Line (and validation Helix), so these are
  recorded as real rare-family deficits rather than hidden with rendered augmentation. Nine
  source-connected shards per split and a hash-verified recovered-base + independent-source union
  path are frozen for execution after the current recovery chain.
- A global plan audit found that the historical train/validation roles, although disjoint in
  glitch ID and 64-second GPS block, share 17 official 4,096-second source files; the independent
  roles share 18, and the four-way union has 59 cross-role source files. These historical roles
  will not be used directly for network training. After materialization, all 3,660 unique glitches
  are frozen again with score-blind `source_component_balanced_v1`, grouping both source-file
  connectivity and GPS blocks before assigning train/validation. The union has 161 indivisible
  components over 380 source files and 1,512 GPS blocks; every one of the 21 families occurs in at
  least five independent components, so source-disjoint family coverage is feasible. A mandatory
  corpus audit rejects any glitch, GPS block, source URL or numeric sample hash crossing the final
  roles before overlap generation.
- The automated dependency chain is deployed without opening test data: independent-source
  expansion (`PID 186221`) → source-component-safe resplit/audit and uniform/family-balanced
  overlap (`187185`) → frozen one-seed promotion (`187534`) → conditional five-seed expansion
  (`187754`) → validation-selected champion plus independent-GPS timing/time-slide calibration
  (`188494`). Each downstream process waits for the exact upstream PID and exits nonzero on a
  missing or failed hash-bound report. The 40-pair candidate endpoint targets 100/year only as an
  engineering gate; it cannot be reported as the final FAR claim.
- The first fresh O4a background shard has 1,393 eight-second windows over 45 GPS blocks, split by
  `hash_threshold_v1` into 668 train, 301 validation and 424 test windows with zero block overlap.
- Nine additional non-overlapping four-pair shards (parent-plan pair indices 4--39) are queued after
  aligned Gravity Spy acquisition. A global 40-pair stable-hash merge and exposure audit will run
  automatically afterward; passing depends on post-DQ validation exposure, not raw file count.

## Continuous-background gap

The fresh validation split has only 301 windows over 10 GPS blocks. Three hundred declared
positive-lag shifts yield 361,200 s = 0.01145 yr of observed background exposure. With zero events,
the 90% FAR upper limit would still be 201.17/yr. This is only 0.0497% of the exposure required for
a 0.1/yr claim. Even the optimistic all-unordered-pairs bound requires 13,479 valid dual-IFO
eight-second windows for a ten-year IFAR-scale claim. The current corpus is therefore an engineering
calibration shard, not a publishable FAR measurement.

## Continuous-background and acquisition update — 11:01 UTC

Six of seven aligned training shards are now finalized. They account for 1,344/1,345 planned rows:
1,081 are usable aligned network samples and 263 are explicit non-finite-companion rejections. The
six immutable reports verify 88/91 source files; the final shard has separately verified the first
of its three O3a files and is downloading the remaining L1 ranges. The 306-row validation bank still
waits for train-cache release. No validation row has been counted as complete.

Fragmented-background exposure now has a second score-blind execution path. A frozen circular
GPS-block schedule pairs every reference block with shifted blocks at the same relative window slot,
requires explicit detector availability on both sides and selects only the shortest nonzero-shift
prefix reaching the predeclared exposure. Execution rejects duplicate, unordered or out-of-range
shifts, re-hashes the background and schedule, enforces the physical-delay plus empirical-timing
window, and reproduces every shift's planned exposure exactly. Candidate search calibration audits
this schedule separately from absolute time slides and still fails publication eligibility when the
target exposure is not reached. Hand-calculated planner, executor and calibration tests pass, as do
the complete CPU suite and Ruff. This improves use of the future 800-pair corpus; it does not turn
the present 40-pair engineering data into a publishable FAR result.

The downstream scale decision is now closed without circular logic. The old 40-pair candidate
pipelines used an unscheduled absolute-slide diagnostic, which correctly made their publication
schedule audit fail but also made the paired 800-pair promotion gate impossible to pass. A new
hash-bound recalibration reuses byte-identical baseline/promoted calibrated candidates and injection
rankings, freezes the common block schedule, and repeats the paired bootstrap/stratum/timing gate.
Only that result can launch the new full-range calibrated streaming wrapper. The wrapper keeps
`test_fraction=0`, requires the complete parent plan, freezes the 0.1/year validation schedule and
fails explicitly if the final exposure remains insufficient; it cannot open locked test data.

## Network OOD execution boundary — 11:27 UTC

The next OOD arm no longer discards aligned companion-detector context. A detector-set embedding
uses a shared H1/L1/V1 Q encoder, explicit binary availability and fixed detector identities; its
dataset rejects a row/array availability mismatch, an unavailable event IFO, changed detector/Q
ordering, non-finite tensors and nonzero unavailable planes. This is the minimum architecture needed
to test whether one-station glitch morphology plus quiet companion detectors improves abstention.

Held-family selection is now automated but score blind. The freezer ranks only unexamined families
meeting predeclared train-row, validation-row and independent validation-block minima, excludes
already opened Blip and Tomte by name, and rejects base train/validation overlap in glitch, GPS block
or official source file. The selected checkpoint and 5% known-abstention threshold remain
validation-only. No new OOD result exists yet: the arm is queued conceptually behind the final
source-component-safe aligned corpus, so the prior Blip/Tomte failures remain the current evidence.

## Train completion and paired-PE controls — 12:58 UTC

All seven aligned training shards are now immutable and merged. The 1,345 planned rows are fully
accounted: 1,082 usable aligned numeric samples and 263 explicit rejections. The reports verify all
91 official source files and cover 1,082 unique glitch IDs over 372 network GPS blocks. The merged
detector subsets are H1L1=550, H1L1V1=481 and H1V1=51. Every accepted row still carries a weak
mask, not a human pixel mask, so this closes acquisition provenance but not the final mask-label
quality gate. The 306-row, 95-source validation acquisition has started at shard 0. Its range files
are growing under PID 203701; no validation report is counted complete yet.

The paired DINGO/AMPLFI path now has a truthful common latency contract. Both backends measure from
model loading and backend-native event preprocessing through posterior and native-result writes,
while excluding artifact verification, imports and mask generation. Each report separately records
model-load, event-preprocessing, sampling and postprocessing/write components. A backend report with
the older sampling-only or mismatched scope fails before publication joining.

`pe-robustness-joint-evaluate` now consumes the two hash-bound batch reports, requires identical
injection/condition sets and complete clean/contaminated/mask-conditioned triplets, then executes
the strict common-input, prior, waveform, detector, hardware, sky-area and latency gates. The
validation promotion configuration requires at least 100 paired injections and 10,000 bootstrap
replicates. RA and polarization bias/coverage use periodic rather than linear statistics. Both
backends must pass coverage, normalized-bias, width, sky-area, effective-sample-rate and latency
checks before `promote_to_locked_test=true`; that decision still cannot make a scientific claim.

No common-prior, validation-selected DINGO model sidecar exists yet. The official O4a DINGO model
manifest identifies the model family and analysis use but does not publish a validation trajectory
sufficient for the repository's common-domain selection gate. AMPLFI has no compatible official
checkpoint; its publication-stage training remains pending. The gate is intentionally not relaxed:
a controlled joint paper result requires verifiable validation selection, most likely a full
common-domain AMPLFI run and either corresponding DINGO training or stronger official selection
evidence.

The official DINGO settings also fail a separate common-prior equivalence audit. Their native
chirp-mass density is `UniformInComponentsChirpMass` on 15--150 solar masses, mass ratio uses the
component-weighted family up to 1.0, luminosity distance spans 100--10,000 Mpc, component-mass
constraints are active and geocentric time is stochastic. The frozen common analysis prior instead
uses plain uniform chirp mass 15--100, plain uniform mass ratio 0.125--0.999 and distance 100--3,100
Mpc. DINGO model metadata and batch execution now fail closed without a hash-bound native-settings
file and passed projection report. Consequently the official posterior model cannot enter the
formal joint comparison merely because it loads successfully.

That incompatibility now has an executable, non-misleading fallback rather than an informal
exception. `dingo-official-native-model-freeze` accepts only the exact four-file acquisition and a
passing dual-model load receipt, then binds the published DINGO 0.5.8 training settings (epoch 225,
10 million waveforms, SEOBNRv5PHM, H1/L1, 16 seconds at 4,096 Hz, 20--1,024 Hz) to a model sidecar.
The sidecar permits within-backend paired robustness while permanently setting common-prior
equivalence and cross-backend absolute comparison to false. The matching validation-only runner
evaluates clean, contaminated and mask-conditioned inputs and calls the single-backend robustness
evaluator; it contains no joint DINGO/AMPLFI invocation. This path remains a smoke until the
official acquisition, model-load receipt and independent paired inputs all complete.

The continuous-background parent size now has a score-blind capacity forecast rather than relying
on the label “800-pair”. A four-pair DQ-verified O4a pilot contains ten validation GPS blocks; all
nine nonzero circular block shifts contribute 2,256 seconds each, for 20,304 seconds total. Linear
yield projection puts the current 800-pair plan near 902 million equivalent seconds versus 726.6
million seconds required for a 90% zero-count upper limit of 0.1/year: only about 24% point-estimate
headroom. `candidate-block-permutation-capacity-forecast` now solves the required block/pair count
under a predeclared safety factor and fails nonzero before the full scorer is accepted if that
margin is inadequate. This is a planning diagnostic; only the exact post-DQ schedule can establish
the eventual FAR exposure.

The exact remote forecast at commit `bab0588` binds pilot schedule hash `2039262...`, background
report hash `dcca79c...` and the 800-pair plan hash `d904333...`. It projects 2,000 validation blocks
and 901,948,800 seconds, which is 1.2413 times the bare requirement but only 0.8275 times the frozen
1.5 safety requirement. The machine recommendation is 2,199 blocks and 880 source pairs; all 880
fit within the 3,309 aligned pairs available. A score-blind prefix-extension path now preserves the
original 800 pair IDs and order, selects only 80 additional pairs from the unscored public inventory
and allows a lineage-aware merge to reuse completed base shards only after every pair-ID hash,
parent hash, scoring identity and split/timing field agrees.

The 880-pair extension is now frozen remotely at commit `6916e64`. Its plan hash is
`f9c2a584baec751588ea6a0aab36eea4c5d42b561fec0aeeeffdb4239a638e5d`; the exact 800-pair prefix
hash remains `8d5e30b...`, and the 80-pair complement hash is `6a18bb0...`. Re-running the same
capacity gate projects 2,200 blocks and 1,091,407,680 seconds: 1.50199 times the bare exposure and
1.00133 times the frozen safety requirement, so the planning gate passes. This narrow forecast pass
does not guarantee the post-DQ schedule; the 880-pair lineage is now the predeclared minimum bank.

The first validation acquisition attempt ended without a completed shard after a public-source TLS
handshake timeout. No row was counted complete. A bounded idempotent recovery reuses the same
`9d6ae24` code, partial cache and output identity, appends rather than replaces the failure log, and
retries each shard before hash-verified eviction. The shared HTTP metadata layer is also being
hardened with bounded retry for transient TLS/408/425/429/5xx failures while permanent HTTP errors
continue to fail immediately.

## Dependency-chain correction — 17:56 UTC

The previously recorded downstream PIDs are no longer queued. They waited only for the preceding
PID to disappear, so a missing validation/recovery artifact caused source expansion, source-safe
resplitting, one-seed promotion, five-seed expansion and the 800/880-pair background wrappers to
fail in sequence. No scientific result or candidate score was produced by those failed launches.
The failure logs are retained. In addition, the candidate background wrapper used process
substitution for five-seed checkpoint selection; a Python failure did not propagate through
`readarray`, leaving an unbound shell array. The wrapper now captures and checks the selector exit
status and exact two-line result before dereferencing it.

Validation recovery remains active. A separate fallback PID waits for the legacy recovery to exit,
reuses any completed shard reports and materializes only missing shards with a `9d6ae24`-equivalent
hotfix whose only production change is bounded GWOSC metadata retry. Recovery can now consume the
source-report inventory from a hash-verified merged report, allowing a mixed legacy/fallback shard
inventory without path-prefix assumptions. The replacement publication chain will be sequential:
each stage must produce and hash-verify its declared report before the next command begins.

## Verified source-cache recovery — 18:20 UTC

The conditional validation fallback exhausted five outer attempts after the official GWOSC archive,
Range GET and detail API all returned HTTP 500/503 or read timeouts. The downstream recovery and
source-expansion wrappers then failed closed on their absent merge inputs; no row or scientific result
was counted. Inspection showed that the blocking 130,114,219-byte O1 HDF5 file was already complete
in the shared cache and had an immutable legacy partial record containing its SHA-256, full HDF5
statistics, sample count and DQ bit sums. The previous fallback nevertheless contacted GWOSC before
consulting that local verification record.

`gravityspy-network-strain-materialize` now accepts an explicit verified-source inventory from an
equivalent interrupted run. Import is allowed only when manifest, config, output duration, worker
count, chunk size and shard identity match. Every source URL and detail URL must belong to the current
shard; the cache slot, byte count, SHA-256, full statistics, strain sample count and DQ bit sums are
revalidated before network access is skipped. The imported inventory path, hash and originating code
commit are retained in partial and final reports. The validation fallback automatically offers a
legacy partial when no completed legacy report exists. Hash tampering and identity drift fail before
materialization.

## Independent-GPS acquisition recovery — 19:05 UTC

The earlier 40-pair O4a acquisition was not complete. Only shard 0 had a passing eight-file batch
report and 1,393 DQ-safe windows; shard 1 had only its four-pair plan. The old runner entered
`background-batch-plan` without first requiring shard 1's download report, then failed with
`FileNotFoundError`. No merged 40-pair corpus or independent-GPS injection endpoint exists, and no
downstream result may claim otherwise.

The eight shard-0 HDF5 files remain intact in the bounded cache. `gwosc-batch-download` can now take
an explicit completed batch report as a verified-source inventory. It matches only sources requested
by the current shard plan and exact cache slot, then independently replays the file SHA-256, full
HDF5 statistics, sample count and DQ/injection bit sums without contacting GWOSC. Imported inventory
hashes are bound into resumable state and the final batch report. The same option is propagated into
the streamed coherent and morphology background paths.

`run_background_acquisition_range.sh` replaces the faulty raw acquisition chain. For the frozen
40-pair range it creates ten non-overlapping four-pair plans, retries resumable downloads, requires a
passing hash-bound report with exactly eight unique IFO files per full shard, and only after every
shard passes builds one global `hash_threshold_v1` background manifest. The calibration corpus is
forced to `test_fraction=0`; the prior shard-0 split is not reused. This stage is model-independent
and therefore can supply independent GPS blocks for the next injection plan without circularly
selecting background using candidate scores.

The 40-pair parent retains the first eight pairs used by the existing batch8 train/validation data,
so a new directory name alone would not establish independent GPS support. A new
`background-disjoint-subset` gate therefore removes every GPS block present in both the existing
10k train and 3k validation manifests, binds both exclusion hashes and recomputes validation live
time. The queued independent-validation runner requires at least 50 remaining validation blocks,
plans exactly 3,000 validation recipes with zero train/test rows, validates the exact recipe set
against direct LALSimulation, materializes scaled-float16 signals, and adds empirical SNR plus
geometric detector-arrival targets. Every stage is artifact-gated; no such endpoint is counted until
the final arrival manifest passes.

A further audit found that using every remaining new validation block for both candidate-threshold
calibration and injection efficiency would still share detector noise across two statistical roles.
`background-purpose-partition` now assigns whole GPS blocks by a frozen hash to
`candidate_calibration` and `injection_validation`, proves complete source-block coverage and zero
cross-purpose overlap, and emits a hash-bound report for each arm. The primary runner requires at
least 25 blocks in each purpose before creating recipes. The already queued unpartitioned runner is
retained only as an engineering transfer endpoint; a new partitioned run will be the publication
candidate and will not treat the engineering result as its primary evidence.

The primary runner now ends by freezing one immutable independent-validation endpoint receipt. It
re-hashes the purpose partition, injection plan, exact-recipe external waveform validation,
materialization, SNR annotation and detector-arrival reports, and exposes only the bound
candidate-calibration background and injection-arrival manifests. The promoted candidate-search
runner no longer re-annotates an arbitrary injection input: it requires this receipt, its exact six
component reports and the two matching manifest hashes before any GPU scoring.

CLI dispatch auditing then found that `--model-selection-report` was parsed but not forwarded into
`run_candidate_validation_pipeline`. The scorer would run, but its final report would have
`model_selection=null` and correctly fail the later paired promotion gate. The argument is now
forwarded and covered by a CLI-level regression test. Any already queued pre-fix pipeline is retained
as non-promotable engineering output; the corrected pipeline uses a separate output identity.

The paired baseline wrapper now applies the same independent-endpoint receipt before scoring the
pre-overlap physical checkpoint. It re-hashes all six components plus the candidate-calibration and
arrival manifests, then runs both baseline inference and the paired promotion command from the exact
scoring code deployment. This closes a path where a baseline could otherwise be evaluated on a
different background/injection pair or the comparison CLI could depend on the caller's ambient
`PYTHONPATH`.

The block-permutation recalibration wrapper now replays the same endpoint receipt and requires both
baseline and promoted pipeline identities to contain the receipt's background and arrival-manifest
hashes plus one common scoring commit. This prevents the later exposure correction from accidentally
joining pipelines that were individually valid but statistically unpaired.

## Independent paired-PE overlap separation

The previous promoted PE smoke reused one `INJECTION_MANIFEST` for two incompatible roles: proving
which validation corpus selected the five-seed overlap checkpoint and supplying the later paired PE
events. Replacing that path with the independent 3,000-injection endpoint would therefore fail the
model-report hash check; relaxing the hash check would instead erase model-selection provenance.

The two roles are now explicit. `MODEL_SELECTION_OVERLAP_MANIFEST` and
`MODEL_SELECTION_VALIDATION_MANIFEST` are used only to replay the selected finetune report.
`OVERLAP_MANIFEST` and `INJECTION_MANIFEST` are the independent paired-PE evaluation inputs and must
be bound to `INDEPENDENT_VALIDATION_ENDPOINT_REPORT`, `INDEPENDENT_PE_OVERLAP_REPORT` and a passing
joint train/validation overlap audit.

`run_independent_pe_overlap.sh` builds that new validation corpus after both the source-safe Gravity
Spy split and independent endpoint exist. It re-hashes all six endpoint components, rejects any
test row, proves that new injection/waveform/GPS and validation glitch/GPS identities do not occur
in the overlap-training manifest, and materializes every detector-compatible validation pair. It
does not synthesize a missing V1 plane when the endpoint injection supports only H1/L1; such rows
are excluded and counted by detector subset. At least 100 compatible rows are required by default.
The final immutable receipt binds the endpoint, arrival manifest, Gravity Spy corpus audit, overlap
configuration, materialized overlap report and zero-leakage joint audit. The generic paired smoke
also records all supplied source receipts in its machine-readable summary.

This implementation is frozen at commit `5fa460c2f59ddb4f882c66c71cc1554f85ac846f` and passed the
complete CPU suite, Ruff, every shell syntax check and diff whitespace validation. The deployment
bundle SHA-256 is `645f3dc1f619a355b4cb0a8419a570a7b136d0afb839a37b7b28a3db119b197e`.
Remote PID `246157` waits for both the source-safe overlap-training manifest and frozen independent
endpoint before building at least 100 detector-compatible validation overlaps. PID `246266` waits
for that receipt and the block-paired candidate comparison before running the corrected three-event
native-input smoke. Both queues fail closed on absent artifacts and currently contain no result.

## Resumable official DINGO model acquisition

The first official O4a DINGO model download retained a valid 10,073,656-byte `.part` file but exited
when Zenodo closed the 4,823,584,930-byte response with curl code 18. Curl's ordinary HTTP retry did
not restart a partial-body failure, so a single CLI invocation could not finish the immutable model
source even though byte-range continuation was already requested.

The acquisition layer now performs bounded outer continuation for declared transient curl exit
codes, reuses the existing partial, checks the frozen expected size before and after every attempt,
and stops after a configurable number of consecutive attempts without byte progress. Permanent
HTTP/curl failures remain fail-fast. A complete partial is verified and atomically promoted without
network access; the final report records attempt count, starting partial bytes and bytes transferred
in the current run before applying the published MD5 and local SHA-256 locks. This changes transport
recovery only and does not make the official model common-prior compatible or publication-ready.

The next gate is also automated. `run_dingo_official_model_load.sh` accepts only a passing
four-source acquisition report matching the frozen config, independently rechecks every role,
filename, byte count, published MD5 and local SHA-256, and then invokes the pinned DINGO interpreter
to load both the posterior and GNPE time-initialization models on CUDA. Its immutable receipt records
both model identities, parameter counts, backend version and environment while keeping
`scientific_claim_allowed=false` and `test_rows_read=0`. Failure under DINGO 0.9.8 now freezes a
separate immutable failure receipt binding the source/acquisition/model/log hashes and exit code.
That receipt does not automatically authorize fallback: its log must first be adjudicated as a
version-compatibility failure, after which only the already declared native-0.5.8 environment may
be tried with the identical model hashes. An unrecorded model or runtime substitution remains
forbidden.

The resumable acquisition implementation is commit `7fd46ba07c40e3095224d9751ea5cde402c4ccf4`
with deployment bundle SHA-256
`a83380167850c808500b342892273652ffc4d442473d387db06a260bb32fb341`. Remote PID
`247229` resumed the retained partial from byte 10,073,656 and is writing a separate acquisition
report, preserving the earlier failed report and log. The dual-load gate is commit
`c0e059cb7ea0c1f43b0e1e517efc777dd56cbccd`, bundle SHA-256
`3576a1559a6765f0006bc716d38da2528c3fb611ce6fb73aed67250a573079c2`; PID `247927`
waits for the passing acquisition receipt before any CUDA load. Neither queued process is counted
as a completed DINGO result.

The machine-readable failure path is commit
`4bf0700d7df84dc2713dacc60ab1f4adf9460b17`, validated by the full CPU suite, Ruff and
all shell syntax checks, with deployment bundle SHA-256
`a732f09498457af735109d75413de7d71c4c64757ebf54397e6b7158e67010b2`. An initial
conditional queue, PID `248500`, carried a mistyped full commit identity. It was caught while still
waiting: its deployment directory was moved intact to
`/root/GW-YOLO-v2-dingo-failure-4bf0700-invalid-commit-env`, so it cannot execute or emit a
scientific receipt. It was not killed. Corrected PID `248600` waits for PID `248500`; if the earlier
`c0e059c` load succeeds it exits without redundant work, otherwise it runs from
`/root/GW-YOLO-v2-dingo-failure-4bf0700d` with the exact `4bf0700d...` identity and writes to the
separate `dingo-official-model-load-audit-4bf0700d` output root.

The native-runtime branch is now specified in code rather than left as an informal recovery step.
`dingo-runtime-failure-adjudicate` replays the failure, acquisition, source configuration, attempt
log and both official model hashes against a committed policy. Only explicit serialization/API
compatibility signatures can pass; infrastructure failures veto a match. The native runner then
requires a passing adjudication and exactly DINGO 0.5.8 before loading the same two model hashes and
freezing a second non-scientific receipt. It is intentionally not queued before a real 0.9.8
failure exists, and it never reads validation or test event rows.

That implementation is commit `f77ffcdd146728192be137e199c42598311b26ad`, deployment
bundle SHA-256 `54108ec5e0e8de18f2ae44b62ec5babe938e39a2d29430fd2287c01a257210b3`.
Remote adjudication PID `249422` waits for corrected load-audit PID `248600`. It exits without
writing if the primary 0.9.8 load succeeded; otherwise it requires the exact structured failure
receipt and writes `dingo-runtime-adjudication-f77ffcd.json`. At 04:30 CST the posterior-model
partial was 1,563,539,000 of 4,823,584,930 bytes; the acquisition and every downstream load remain
pending, not completed evidence.

AMPLFI training background is now independently capacity-gated. The committed policy measures
physical contiguous seconds and GPS blocks, not rows: at least 200,000 seconds/50 blocks for train
and 50,000 seconds/13 blocks for validation. It excludes test strain and fails nonzero after writing
a machine-readable negative report. A preliminary metadata count of the existing 64-block O4a
context-safe corpus is only about 9,544 training seconds and 3,232 validation seconds, below even
the AMPLFI configuration's 10,000-second validation floor and far below the new publication target.
The old one-epoch AMPLFI queue therefore remains engineering-only and cannot be promoted.

The exact audit at commit `be5724099eb40916e2c7a683cd468e31da010289` confirms those counts:
train is 9,544/200,000 seconds across 38/50 GPS blocks; validation is 3,232/50,000 seconds across
13/13 blocks. It excluded 408 test metadata rows and read zero test strain arrays. The negative
report SHA-256 is `457ce2dc8640a4a490965a9026c211654fa1e3590f372a584e6d7ad579ca6f0e`;
deployment bundle SHA-256 is
`4aa33fe817e2d6686138f5828908f7c8fc42d3e8bf8e7a435a8b6c0c3c7eeb32`. A first invocation
carried a mistyped full commit environment value; that report was moved intact to
`amplfi-background-capacity-be57240.invalid-commit-env.json` and is excluded. The corrected report
contains the exact commit above. Future remote invocations inject `git rev-parse HEAD`
programmatically rather than transcribing full hashes.

The executable scale response is an 80-source-pair AMPLFI training acquisition plan selected from
the current O4a H1/L1 inventory only after excluding every pair ID and file GPS start in the frozen
880-pair candidate/independent-validation plan. The new `gwosc-plan-disjoint` command replays the
excluded plan against current public metadata, selects the complement score-blindly, and records
both pair-ID and GPS hashes. This prevents the larger PE background from leaking validation noise
back into AMPLFI training.

The frozen plan now exists at commit `73a7893c9758a2e1e843ff6a6684f0dfcc155267`, bundle
SHA-256 `a2ad27ccf42a983e0dbce91f5437889e4c95d2b75856aba6a79cfefc84429c1e`.
It replayed the 880-pair exclusion at SHA-256 `f9c2a584...`, found 3,309 aligned O4a H1/L1
pairs, removed all 880 reserved file GPS starts, and selected 80 from the 2,429-pair complement.
The immutable plan SHA-256 is
`0752994e308238ec109495c694a6e19cd36d09dd5449f61534e008058dfa3820`; selected pair-ID
and GPS-start hashes are `235b48f4...` and `185e2ad3...`. No download has started yet: with only
23 GB free on the external volume, retaining all 160 source HDF5 files alongside the roughly
10 GB downsampled AMPLFI bank and the official DINGO models would be unsafe. Acquisition must
therefore stream small shards, freeze each export, and provenance-evict verified source copies.

The storage-safe executor is now implemented. It downloads four pairs at a time, produces only
stable-hash train/validation windows, exports numeric 2,048 Hz H1/L1 data, verifies complete export
coverage, writes an immutable deletion intent and then removes only the exact public cache files
under the declared cache root. The source URLs and plan remain hash-bound and recoverable. A final
20-shard merge verifies complete parent-plan coverage and must pass the capacity audit before any
AMPLFI training job can start.

The executor is commit `b8ace2291cba802aefc9d75c58eacd3ff2a4fcd1`, deployment bundle
SHA-256 `b1eb6f117d6d2feb0cd7266803db8a2d3f7c830435644cd5800d24b27829691c`.
Remote PID `251986` is queued behind official DINGO acquisition PID `247229` and the higher-priority
independent 40-pair GW-YOLO validation acquisition PID `240208`, avoiding concurrent bulk network
and disk pressure. Its final output is accepted only if all 20 shard receipts merge and the frozen
capacity policy passes; the queued state is not counted as acquired background or AMPLFI evidence.

The downstream AMPLFI training budget is now predeclared rather than launched with ad hoc CLI
overrides. Publication stage 1 contains 100 epochs, 20,000 updates and 5.12 million online waveform
examples. Its runner replays the capacity-ready bank and every exported file hash, supports an
unambiguous `last.ckpt` resume, requires at least 100 completed epochs and 50 validation points,
loads the selected AMPLFI model/scaler on CUDA and freezes common-prior model metadata. The much
larger 800-epoch stage 2 is blocked until stage-1 validation evidence justifies compute scaling.

Stage-1 orchestration is commit `add14951c35bfc0d33d12213cd1eb621b8e691ad`, deployment
bundle SHA-256 `7b86d949ce16a6ec4ad37a7562380a01525cad56ef00d9edc399a0c769ddf492`.
Remote PID `252994` waits for the exact capacity-ready receipt from PID `251986`, then uses pinned
AMPLFI 0.6.0 and waits for an idle GPU. Its selection, CUDA load and model-metadata gates all remain
pending; the queued job is not a trained model or posterior result.

## Official DINGO native-prior robustness handoff

The official acquisition and CUDA load gates have now completed. The four-source acquisition
report SHA-256 is `1503c428449b8b94d23a4c2f08218e500f45cc7480c175518675cdd11e6bff3f`.
DINGO 0.9.8 loaded both published networks on the RTX 4090 D in 9.53 seconds: the posterior model
has 401,491,660 parameters and SHA-256
`6917a210cabeb71792be2e7b6c849220d95a6e965bd3a51103692916dc6c2054`; the time model has
88,989,753 parameters and SHA-256
`5c89427be118abf9fd49ef186b075bf9c8f0963bdcff7c4dd607c2d893f646de`. The immutable passing
receipt SHA-256 is `4f1162cff50af437b18d95b412ceb4ff26270451fdbd359bf0b4eb3d35eb09f5`.
This is compatibility evidence only, and the runtime's backward-compatibility warning reinforces
the need for the separate native-prior robustness label.

Commit `1eb892bfddc9994d3b0ac720da590299418fceef` added that label and its validation-only runner.
The exact Git bundle SHA-256 is
`de0e1b7b05b83f2029197fc91f90da3cc496dc97defdfca77e4ba3b88507f695`. An initial queue PID
`255395` accidentally contains literal `+` command prefixes; it is retained, will fail before
creating an output directory, and is excluded from evidence. A subsequent review caught that
corrected PID `255456` would still have used DINGO 0.9.8 for posterior generation. Its exact code
deployment was therefore recoverably moved to
`/root/GW-YOLO-v2-dingo-native-1eb892b.load-only-incompatible-runtime`; the waiting process was not
terminated, but its `cd` must fail before inference. Neither PID can produce accepted evidence.

The corrected scientific gate now requires the inference and training runtime versions to match
exactly, and rechecks the event runner's reported version on every condition. The committed native
runtime lock is DINGO tag v0.5.8 at commit
`04c8ab3ec694410ad85466ea6bfdc6aa2274ac14`. Its installer creates a separate package overlay over
the already frozen dependency environment, uses `--ignore-installed` so the base DINGO package
cannot be mutated, records both sorted package inventories, and requires CUDA. Only a passing
0.5.8 dual-model load receipt can create native model metadata or authorize posterior generation.
The first remote overlay attempt failed closed before promotion: it exposed only the outer venv's
package directory, so Torch was absent, and source-build version discovery fell back to `0.0.0`.
It wrote no runtime receipt and did not modify the 0.9.8 base. The corrected installer exposes all
base-runtime site-package directories and supplies the version to setuptools-scm only after the
exact v0.5.8 tag/commit and clean checkout have been verified.
A second attempt then exposed all dependencies correctly but still failed closed because disabling
build isolation also removed setuptools-scm from the build context; the wheel was again labelled
`0.0.0`. The next revision pins setuptools-scm 8.1.0 inside the overlay before building, records
that build-tool version, and retains the exact-version post-install rejection. Neither failed
overlay was promoted or used to load a model.
The native overlay itself then passed under DINGO 0.5.8 with CUDA, but the first model-load call
failed before touching model contents because the repository smoke imported the post-0.9
`dingo.core.posterior_models` API. DINGO 0.5.8 exposes `dingo.core.models.PosteriorModel` instead.
The load smoke now dispatches only between exact supported versions 0.5.8 and 0.9.8 and rejects any
other API version; parameter counts are taken from the corresponding native network attribute.
The same exact-version dispatch is now applied to the per-event GNPE runner; both supported DINGO
versions retain the common event-dataset and sampler contract, while model construction follows
the native API and records which loader was used.

The corrected native overlay has now passed on the RTX 4090 D. Its receipt SHA-256 is
`01366de9751cdbad0c44008300d5437ca8694ba3030a203c65d33f397d76c275`; it records DINGO
0.5.8, source commit `04c8ab3...`, setuptools-scm 8.1.0, Torch 2.13.0+cu130, CUDA availability,
and both dependency inventories. The exact 0.5.8 native API then loaded the same two official
model hashes and reproduced parameter counts 401,491,660 and 88,989,753. Its immutable load receipt
SHA-256 is `a7ec6a3f2dfb58513fb3274c71b732192823adad5096ad08fd2ec4464e253a98`.

Per-event native API support is commit `1013f9a688783df09e07d0a1f9f4b01ae8ec0139`, deployment
bundle SHA-256 `02f20a6f89d3156168a88f9ae19838222277e1ec0142762680d71b5c402e07c4`.
Remote PID `259591` is now the only valid official-native posterior queue. It waits for independent
paired-input smoke PID `246266`, requires the 0.5.8 load receipt above, and remains validation-only
and barred from cross-backend absolute comparison.

The first deterministic zero-event runtime smoke then failed before inference because DINGO 0.5.8
imports `scipy.signal.tukey`, while the frozen modern SciPy exposes the same implementation at
`scipy.signal.windows.tukey`. The native event runner now installs that exact-symbol alias before
importing the 0.5.8 sampler and records the shim in every inference report; no numerical
implementation is substituted. The failed smoke contains no detector or test data and no posterior.
A second synthetic smoke reached the real GNPE sampler and exposed an error in the smoke fixture:
it retained bins through the 2,048 Hz Nyquist frequency instead of the model's 1,024 Hz maximum.
The production conditioning path already crops at 1,024 Hz; the synthetic fixture is now aligned to
the same 16,385-bin grid before the model's 20 Hz crop.

The corrected end-to-end synthetic event smoke now passes under DINGO 0.5.8. It loaded both exact
official models, ran one GNPE iteration for four samples on CUDA, wrote numeric posterior and native
HDF5 results, and recorded the SciPy symbol alias. Model load plus preprocessing, sampling and writes
took 8.31 seconds; this tiny smoke is not a latency benchmark. The immutable summary SHA-256 is
`775df53e271cab079366454be46a9a90962d2c6e2cb7bf00cdc668341b530ef2` and remains explicitly
non-scientific. The earlier PID `259591` code deployment was recoverably quarantined because it
lacked the alias. A further provenance review found that PID `261512` checked the passing synthetic
runtime summary only in its outer shell, while its frozen model metadata did not bind that receipt.
Its deployment was therefore recoverably moved intact to
`/root/GW-YOLO-v2-dingo-native-8215ebd.missing-internal-runtime-evidence-gate`; the process was not
terminated and must fail before inference after its upstream wait ends.

Commit `d49fe07a496f0dd0cedd7c8d8742f6f154c19d96` closes that gap. Official-native model
metadata now requires and hash-replays the exact 0.5.8 overlay receipt, all three package/config
inventories, the synthetic event summary and inference report, the posterior and native-result
files, both official model hashes, native loader API and recorded Tukey compatibility alias. Those
receipts are also mandatory batch artifacts and enter the batch run identity. The full CPU suite,
Ruff and all shell syntax checks pass. Deployment bundle SHA-256 is
`de4b5a01e13ba1ef3244fde523a317c440d4e25984e38c3b0757992ccf09aed1`.
Remote PID `262995` is the current valid validation-only official-native posterior queue; it waits
for the safely invalidated predecessor and independent paired-input PID `246266`, then reads the
same passing overlay/load/event receipts before any inference.

## Aligned validation completion and current data frontier

The isolated Gravity Spy validation acquisition has now completed all seven shards. The verified
merge contains 287 unique validation glitches over 113 network GPS blocks with detector subsets
H1/L1/V1=179, H1/L1=76, H1/V1=24 and L1/V1=8. Its manifest SHA-256 is
`077f9123be02aee7e8b27aad30fc3616f65fa890a09ef1d391ae268f94fafd0e` and source-report
inventory hash is `557bd93428df4bd9d2152a94fb11b4fafb74ed6c98dfea7a459c9fbd86ec5b7b`.
Nineteen of the 306 planned rows were explicitly rejected for non-finite companion-detector strain
(nine L1 and ten V1); none were silently converted to zeros. All 287 retained labels are still weak
masks, so this is data-completeness evidence rather than a segmentation claim.

The artifact-gated detector-set recovery has started from that merge and is materializing the
expanded training source inventory before source-component-safe resplitting. In parallel, the
GPS-disjoint 40-pair O4a background has completed 5 of 10 four-pair shards (20 of 40 H1/L1 pairs).
The last completed shard was frozen at 06:01 CST on 2026-07-22. At that checkpoint the external
volume retained 17 GB free; AMPLFI bulk streaming remains queued behind this higher-priority
acquisition and retains its 8 GB free-space gate.

## Physical overlap data-scaling controls

Commit `3c138d2f17ee4fd8ab7263f6a61eafb17fc7f019` adds the missing real-glitch data-size
experiment. It freezes strictly nested 250/500/1,000/full mixture prefixes by stable rank over all
waveform/injection, glitch and both GPS-group identities, re-hashes every numeric sample and the
source-component-safe corpus audit, and keeps one immutable validation endpoint. Five paired seeds
must run under both 20 fixed epochs and exactly 4,000 optimizer updates. The summary uses paired
seed bootstrap intervals; more same-distribution data is promoted only when both controls show at
least 0.01 glitch-IoU gain with a positive lower bound and retain at least 95% clean chirp IoU.

Commit `7b8816ca7803da775dae368ec35e4c39c75e77d7` makes the exact-update regression use CUDA
when available. A synthetic-tensor RTX 4090 D smoke stopped at exactly one requested optimizer
update; its non-scientific receipt SHA-256 is
`ff965c6f8c841400627a3361aa0706c6499687d2c5535f9b367ed517847db023`. The exact deployment
bundle SHA-256 is `af720a9f5037e7df2373ee0d449051738332ce6c66156f583bc325dfbd93e1f8`.
Remote scaling PID `265644` is queued after the primary five-seed overlap chain and the currently
scheduled DINGO/AMPLFI validation work, preventing GPU contention. It cannot start unless the
five-seed summary, source-safe train/validation overlaps and expanded corpus audit all exist.

## Ordered remaining work

1. Finish and audit aligned H1/L1/V1 Gravity Spy train/validation materialization.
2. Build network-aware chirp+glitch overlaps and train a shared per-IFO encoder with detector
   validity masks, dual instance masks and a clean non-inferiority gate.
3. Replace the failed OOD baseline with a validation-calibrated energy/distance or contrastive arm,
   then evaluate held-family and observing-run transfer without retuning.
4. Expand independent O4a GPS-block background to at least the frozen IFAR exposure target, score
   every retained candidate, fit the threshold on validation only, and freeze it.
5. Run injection efficiency, FAR/IFAR, sensitive distance and `<VT>` with paired bootstrap intervals
   and detector/glitch/SNR/population strata.
6. Promote only a gate-passing architecture to at least five seeds.
7. Compare paired raw, contaminated and mask-conditioned inputs with DINGO/AMPLFI-compatible PE
   using identical priors and waveform assumptions. Report coverage, bias, width, sky area, sample
   efficiency and latency; do not compare PE systems through detection mAP.
8. Freeze code/config/data/model/threshold/OOD hashes, then perform the one-time locked O4b/GWTC-5
   evaluation and prepare the PRD/JCAP/ApJS manuscript package.

The separate 5,000-injection O4a endpoint currently being materialized is a development-promotion
endpoint, not O4b/GWTC-5. It will remain unopened until the data-axis comparison, model checkpoint,
validation threshold and OOD policy are all frozen and the one-time opening receipt can be written.

## Schedule estimate

Assuming the remote GPU remains available and GWOSC downloads remain stable:

- remaining aligned train acquisition: roughly 3--10 hours at the observed variable GWOSC rate;
- aligned validation acquisition and final diversity audits: a further 8--30 hours, so 1--2 days
  end to end with a 3-day conservative allowance;
- network-aware overlap baseline and OOD replacement: 3--7 days;
- background expansion plus executable FAR/IFAR/`<VT>` benchmark: 1--3 weeks;
- five-seed promoted model and deglitch ablations: 1--2 additional weeks;
- paired DINGO/AMPLFI inference and calibration study: 2--4 additional weeks;
- locked evaluation, robustness tables and manuscript-quality reruns: 2--4 additional weeks.

The earliest credible end-to-end evidence package is therefore about 6--8 weeks; a conservative
paper-ready estimate is 8--12 weeks. These are execution estimates, not a promise that a positive
effect will pass the frozen gates. A negative gate result triggers a bounded redesign and may extend
the schedule. Public O4b/GWTC-5 strain availability is an external dependency for the final locked
claim.
