# GW-YOLO research status — 2026-07-22 07:20 UTC

## Executive decision

The repository now has a nearly complete fail-closed implementation for validation evidence,
locked O4b/GWTC-5 search, OOD transfer, paired DINGO/AMPLFI parameter estimation and catalog
diagnostics. This is engineering readiness, not a scientific result. The real publication ledger is
still only 1/10 on validation evidence and 0/9 on locked-final evidence. O4b/GWTC-5 remains unopened.

The immediate scientific bottleneck is the source-file/GPS/glitch-disjoint aligned H1/L1/V1 corpus.
No training, threshold, calibration, OOD or PE decision is allowed to bypass that corpus audit. The
remote DAG is running and all downstream jobs wait for immutable upstream reports rather than
polling for partially written data.

A new model-level PE compatibility audit also closes a previously implicit boundary: the official
DINGO O4a checkpoint is valid for within-DINGO paired robustness, but its native prior and waveform
contract prohibit an absolute DINGO-versus-AMPLFI join. The joint runner now fails before GPU
inference unless both validation-selected sidecars replay to the same canonical prior, analysis and
native-model waveform assumptions, source contract and common parameter set.

## Progress by workstream

Percentages are stage-local completion estimates. They deliberately do not average code that exists
with scientific evidence that does not yet exist.

| Workstream | Current completion | Evidence at this snapshot | Next hard gate |
|---|---:|---|---|
| Reproducible code and provenance | 97% | Stable CLI, atomic reports, commit/config/manifest/model hashes, CPU tests and immutable endpoints exist; publication background and both PE comparison scopes are fail-closed | Complete real backend/integration replays and paper-table automation |
| Locked O4b/GWTC-5 protocol implementation | 98% | Suite v2 freezes 14 post-access inputs and 9 outputs, including matched-event within-backend PE portfolio; one-time access remains gated | Validation ledger must reach 10/10 before access |
| Independent aligned Gravity Spy acquisition | 39% by shard (7/18) | 7 completed training shards; shard 7 is active | Complete 9 train + 9 validation shards, merge and pass zero-overlap audit |
| Independent training-bank coverage | 65% of planned train rows already usable (1,075/1,653) | Completed reports account for 1,355/1,653 planned train rows; active shard excluded | Finish remaining train rows; retain explicit rejects and detector downgrades |
| Purpose-disjoint O4a validation endpoint | 100% | 3,000 frozen injections; 45 calibration and 59 injection-validation GPS blocks with zero overlap and zero test rows read | Reuse this exact endpoint for model/search/PE validation without refreezing |
| Source-safe overlap/model selection | 10% execution, 100% queued | Full DAG and promotion rules are deployed; it is waiting on the corpus audit | One seed gate, then five seeds without test access |
| Fixed-epoch/fixed-update data scaling | 5% execution, 100% protocol | 250/500/1,000 physical-group curve and five paired seeds are queued | Both controls must improve the frozen endpoint before scaling |
| Continuous-background FAR/IFAR/`<VT>` | 10% execution, 95% infrastructure | The original 800+80 plan failed purpose-disjointness; a corrected 880-pair plan passed zero-overlap and capacity audits and waits on model promotion | Execute only the corrected plan and reach at least 23.02585093 equivalent years at the 0.1/year endpoint |
| Raw versus mask-conditioned search | 10% execution, 95% infrastructure | Paired streaming, common-FAR comparison and clean non-inferiority gates are queued | Positive or null result on identical injections/background, retained either way |
| Detector-set OOD/run transfer | 10% execution, 95% infrastructure | Frozen detector-set logit-energy scoring and locked transfer endpoint exist | Validation-only held-family/run result with no unknown-score selection |
| Calibration perturbation robustness | 10% execution, 95% infrastructure | Seven physical time-domain scenarios and fixed-threshold evaluator are queued | Scenario thresholds remain frozen; timing/efficiency strata pass |
| DINGO within-backend comparison | 50% | Official O4a model source, native runtime load and event smoke are verified; strict paired repair queue is live | Complete raw/contaminated/mask-conditioned validation batch and uncertainty report |
| AMPLFI within-backend comparison | 22% | v0.6 environment, group-safe data bridge, 100-epoch/20,000-update stage-1 and independent paired successor are queued | Complete validation-selected model, then identical-event paired batch |
| Matched-event PE portfolio | 100% protocol, 0% evidence | Validation/locked evaluators, 100-event promotion, 10k bootstrap and ledger contracts are implemented; no posterior result has completed | Aggregate both strict within-backend batches; absolute backend ranking remains forbidden |
| Absolute DINGO/AMPLFI join | 0% evidence, 100% gate | Official DINGO declares `common_prior_equivalent=false`; planned AMPLFI also uses a different native training waveform | Train/freeze genuinely matched models or retain only within-backend paired deltas |
| Publication validation ledger | 10% (1/10) | Last real audit has one passing item and nine pending items | 10/10 before suite freeze/access |
| Locked-final ledger | 0% (0/9), intentionally | No O4b/GWTC-5 event-level strain or score has been opened | One-time evaluation only after validation 10/10 |
| Paper-ready scientific evidence | about 12% | Strong negative/diagnostic results exist; primary fixed-FAR claim does not | Five seeds, source-safe scaling, FAR/`<VT>`, OOD/calibration and PE evidence |

## Physical data accounting

The seven completed independent-source training shards contain:

| Quantity | Count |
|---|---:|
| Requested physical rows | 1,355 |
| Usable aligned numeric H1/L1/V1 detector-set rows | 1,075 |
| Explicit rejected rows | 280 |
| Unique glitches | 1,075 |
| Unique network GPS blocks | 601 |
| H1+L1 rows | 502 |
| H1+L1+V1 rows | 490 |
| H1+V1 rows | 52 |
| L1+V1 rows | 31 |
| Runtime detector-set downgrades | 83 |

These counts are independently replayed in the immutable progress snapshot
`train-progress-f5864f9-20260722T051633Z.json` (SHA-256
`ee53e7f21ad290af11e4e33d8decb1d2ef166f6b8ef9572b612f925994d9fb57`). The snapshot binds code
commit `f5864f9e029c559083a58fc0bdfc4b7389725d36`, the frozen plan manifest and all seven completed
report hashes. It records zero planned source-file overlap between shards, 81.97% physical-row
accounting completion and 77.78% training-shard completion. It intentionally sets
`scientific_claim_allowed=false` and `partial_corpus_may_select_model=false`.

Every usable row is numeric multi-Q data with shape `(3 IFO, 3 Q, 96, 96)` and Q values 4, 8 and
16. These are physical rows, not rendered-image or augmentation counts. The current masks are weak
metadata-derived masks; their reports explicitly forbid a network-coherence or scientific claim
until human mask audit and continuous-background evaluation pass.

Shard 7 has now hash-verified 11 source files, including the completed 129.8 MB O3a L1 source, and
has advanced to an O3b H1 source with 62,123,068 bytes staged across eight live range downloads. The
process retains active HTTPS sockets. Root storage has about 8.4 GB free and the external data
volume about 11 GB free. Source caches are
evicted only after hash-verified numeric outputs are finalized.

## Frozen independent O4a endpoint

The separate GPS-disjoint validation acquisition is complete. Its immutable endpoint report is
`independent_validation_endpoint_report.json` at SHA-256
`7274d6ce800f9599f1341d03c8c31e937cc6a64dcb963b761d9cf83e39fc8c67`. It contains 3,000
externally validated, 2,048 Hz, 64-second injection scenes; the float16 storage reconstruction
relative L2 error is at most `2.2028026202831435e-4`. Candidate calibration uses 45 unique GPS
blocks and injection validation uses 59 different GPS blocks, with zero purpose overlap and zero
test rows read. The network-SNR distribution deliberately includes a hard tail: 1,652/3,000 rows
are below SNR 4, while 143 rows are at or above SNR 15.

The strengthened publication-readiness replay at
`publication-readiness-9586bfa-20260722T053845Z.json` (SHA-256
`80a8fb13f973e1cc2e0db20081bd076e6130f4becd7ca423e6bd615877689ca8`) now independently enforces
exactly 3,000 rows, at least 25 GPS blocks for each purpose, ten endpoint predicates and eight
artifact replays, including all six component reports. All checks and replays pass on the real
endpoint. The other nine requirements remain pending, so the validation ledger remains honestly
1/10. This endpoint is validation-only and cannot authorize a sensitivity claim or locked-corpus
access.

## Continuous-background plan correction

An interval-level audit found that the earlier 800+80 O4a background plan is not independent of the
frozen validation endpoint. The retained failure report
`validation-purpose-overlap-audit-c3f0b89.json` (SHA-256
`00db63a0423d8f68f3dd2e4a442a19bf8916ecf350c94b666fbfb41b49fa5c3b`) records 34 overlapping
source-pair IDs and 104 overlapping 256-second GPS blocks: 45 candidate-calibration and 59
injection-validation interval hits. Its exit status was non-zero by design. Any eventual products
from that already-waiting legacy chain are diagnostic-only and must not enter the publication
ledger.

The replacement score-blind O4a plan contains 880 H1/L1 source pairs and explicitly excludes all
40 source pairs used by the ten independent-endpoint acquisition shards. Its plan SHA-256 is
`20cf0cf3e66baa19e0a5439c2a11cd71a89f5dcc88ea88e68f9631b86fac4430`. The immutable purpose
audit `validation-purpose-audit-c3f0b89.json` (SHA-256
`7de2812f92fb9c1faa71458a65777bf51443598a16f3bf5e555336a5cfcc021d`) passes with zero direct-pair
and zero GPS-interval overlap for both validation purposes. No candidate score or test datum was
inspected.

The corresponding score-blind capacity forecast `capacity_forecast-c3f0b89.json` (SHA-256
`7ad55856939d081dbddc670457929c8d297b38351215407277c2cebac338b289`) projects 2,200 GPS blocks,
2,199 nonzero block permutations and 1,091,407,680 seconds of maximum equivalent live time. This is
1.50199 times the 90%-confidence zero-count requirement at FAR 0.1/year, or only 1.001327 times the
predeclared 1.5x safety requirement. Therefore the publication runner now fails closed unless the
endpoint, zero-overlap audit, plan and capacity forecast replay by hash and match the exact bounded
220-shard run.

## Remote execution DAG

The active chain is:

1. finish independent train/validation source shards;
2. merge the recovered and independent banks, resplit by connected source/GPS components and audit
   zero overlap;
3. generate source-safe clean+glitch overlaps;
4. run the frozen one-seed promotion, then five seeds only if it passes;
5. compare the promoted candidate model against the baseline on the independent O4a endpoint;
6. run fixed-epoch and fixed-update scaling controls;
7. stream the corrected, endpoint-disjoint 880-pair background in a separate output root;
8. execute raw/mask, calibration, OOD and paired-PE validation;
9. aggregate the 10-item validation ledger;
10. only then freeze and open the locked suite once.

At the snapshot the root acquisition process and all declared waiters are alive. GPU utilization is
zero because the only runnable stage is network/HDF5 acquisition. No unrelated process has been
terminated and `/root/GW-YOLO`, its weights and historical `runs` directory remain untouched.
The two legacy-background waiters also remain alive because process termination was not authorized;
their outputs are quarantined by evidence policy rather than silently deleted or promoted.
The corrected background queue is live as PID 19267 and waits fail-closed for the legacy chain to
exit before using commit `399039227f094f09c0349d42c59635ab3a58b246` and the separate output root
`candidate-purpose-disjoint880-3990392`. PID 19317 is a second fail-closed waiter that will bind the
resulting 10,000-bootstrap calibration to the frozen independent endpoint. Neither waiter currently
consumes GPU, network or test data.

A downstream dependency audit also found that the already-running mask background queue names the
old 800-pair plan. That raw/mask result is therefore quarantined with the old baseline. The current
implementation now produces a shared immutable background-plan authorization receipt and requires
both baseline and raw/mask runners to replay it. A replacement mask queue will use the corrected
880-pair plan and a distinct output root after its validation-only mask timing gate passes.
The real authorization receipt is
`publication_background_plan_authorization-d9a0434.json` at SHA-256
`08cb8e9579db5d1559fe112f4de1736f9a764575e0d9aa512858840f6406d022`; its stable authorization ID
is `d47c1d59dff6a10f2c04e7785b900efac35d0b23cef922dced9318cfe7b26b38`. PID 20288 is the corrected
mask-background waiter. It requires both the corrected baseline and the mask timing receipt, and
will write only to `mask-background-purpose-disjoint880-d9a0434`.
PID 21738 then waits for that immutable receipt and runs
`candidate-search-raw-mask-endpoint-bind` at commit `b56493e52f512a087d15b207a0843ea078171363`.
The publication ledger now accepts only this bound evidence, which replays the source receipt,
authorization, parent plan, both arm merges, both calibrations, paired comparison and mask
validation/timing receipts. Neither the old standalone comparison nor an unbound runner receipt can
pass the `paired_raw_mask_vt` requirement.

The paired-PE chain is also live but deliberately split by scientific scope. PID 2488 waits for the
independent overlap and produces the common input smoke; PID 2518 then runs the official DINGO
within-backend triplets. PID 2810 waits for the source acquisition before exporting the group-safe
AMPLFI background, and PID 2863 runs the validation-selected AMPLFI stage-1 model. There is no
deployed absolute joint queue because the currently available official DINGO sidecar explicitly
fails the common-prior/native-waveform boundary. Deploying such a queue would manufacture an
invalid comparison rather than accelerate the paper.
The corrected successors use isolated checkout commit
`c32f91428475a51d93545b81ad89dc106867b0ca`. PID 23285 waits for the old DINGO attempt and then
replays its resumable posterior state under the strict within-backend evaluator with 10,000
bootstrap replicates. PID 23288 waits for both the paired input smoke and AMPLFI stage-1 model, then
runs the independent AMPLFI three-condition batch and strict within-backend report. Both queues use
new or explicitly resumable output roots and read validation rows only.

The required publication path is now the stricter matched-event portfolio, not an invalid absolute
join. It replays both posterior manifests, requires identical injection/condition keys, common
source-input bytes and truth, and recomputes every paired statistic with the same promotion policy.
Each backend must hold its own model, prior, waveform and detector identity fixed across clean,
contaminated and mask-conditioned conditions. Validation requires at least 100 common injections
and 10,000 bootstrap replicates. Locked suite v2 preserves the same per-backend identities on O4b
and writes `absolute_cross_backend_comparison_allowed=false` into the final endpoint.

## Conditional ETA

These ranges are compute/network estimates, not promises that a promotion gate will pass. A failed
gate intentionally causes redesign rather than silently advancing.

| Milestone | Earliest plausible | Conservative if infrastructure is stable | Main uncertainty |
|---|---:|---:|---|
| Finish 18 independent source shards | 12 h | 36 h | GWOSC range-download throughput/retries |
| Source-safe merge, resplit and overlap materialization | +2 h | +8 h | Usable-row rejection rate and disk I/O |
| One-seed gate plus five-seed model | +18 h | +60 h | Gate result and GPU training time |
| Fixed-control scale curve and candidate promotion | +1 d | +3 d | Five seeds at three physical scales |
| 880-pair background and validation search evidence | +2 d | +6 d | GWOSC acquisition and achieved post-DQ exposure |
| Calibration and OOD validation | +0.5 d | +2 d | Held-family support and timing gate |
| DINGO/AMPLFI paired validation | +2 d | +7 d | AMPLFI 100-epoch/20,000-update stage and backend throughput |
| Validation ledger 10/10 | about 4 d | 10 d if all gates pass | Scientific promotion failures can extend this to weeks |
| One-time locked evaluation after 10/10 | +2 d | +5 d | O4b acquisition, PE runtime and exact background exposure |
| First complete paper draft after locked evidence | +2 weeks | +4 weeks | Statistical result strength and required ablations |

The optimistic submission window is approximately four to eight weeks only if the source-safe model
passes its frozen gates and the background reaches the predeclared exposure. A negative gate is a
valid research result but makes that calendar estimate obsolete. The code must not claim that simply
waiting or increasing schedule length guarantees AMPLFI/DINGO-level results.

## Definition of success

Success is not catalog hit rate or image mAP. The primary endpoint is paired change in recovered
`<VT>` at common frozen FAR on continuous background, with clean-injection non-inferiority and paired
bootstrap uncertainty. The distinct paper contribution is mask-preserving, detector-set-aware
glitch robustness and improved downstream posterior reliability under contamination. DINGO and
AMPLFI are compared only on shared PE coverage, bias, width, sky area, sample efficiency and latency,
never as detection-mAP baselines.

No claim is currently authorized. The locked corpus remains closed until all ten validation reports
replay by hash and pass their predeclared predicates.
