# GW-YOLO research status — 2026-07-23 13:17 UTC

## Executive status

The project is approximately **38% complete against the publication objective**. This estimate
separates implementation from evidence: reproducible engineering is approximately **99.5%**, the
validation-to-locked pre-access protocol is approximately **99.0%**, but accepted paper evidence is
only approximately **15%**. The pre-access percentage was reduced after a direct physical-count
audit found that the current 3,000-injection independent endpoint contains H1+L1 only; it cannot
support a variable-detector calibration-robustness claim. The O4b/GWTC-5 locked-final ledger
remains **0/9 by design**, because the
validation ledger is not yet 10/10 and the access log is still absent.

No claim of superiority to AMPLFI or DINGO is currently supported. The frozen comparison is
within-backend and paired across clean, contaminated and mask-conditioned inputs. The intended
distinct advantage remains mask-aware deglitching and search/PE robustness under overlapping
artifacts, not clean-data detection mAP or an absolute posterior-backend ranking.

## Live machine state

At 13:17 UTC the remote machine had 7.9 GiB free on the root volume and approximately 3.4 GiB free on
`/root/autodl-tmp`. The external volume remains below the frozen 8 GiB execution guard. All 20
AMPLFI background shards (80 H1/L1 source pairs) have downloaded, exported and evicted their
verified source HDF cache. The result is deliberately not marked complete: the capacity gate
measured 195,408 train seconds against the predeclared 200,000-second minimum, a deficit of 4,592
seconds or 2.30%. Train GPS blocks (791) and both validation requirements (55,496 seconds and 228
blocks) pass. A small source-disjoint extension is required; rerunning the 80 pairs is not.
GPU 0 (RTX 4090) is idle because the critical path is network metadata/data and guarded disk
reclamation, not training compute.

PID `156550` is the source-safe overlap chain. The AMPLFI process has ended, but this successor
still waits for at least 8 GiB of external free space, then runs the one-seed arm comparison and
conditionally the five-seed gate. PID `168735` is a non-destructive successor: it retains OOD evidence regardless
of promotion and runs the fixed-epoch/fixed-update scaling matrix only if the five-seed gate passes.
PID `175740` is the expanded, purpose-disjoint 880-pair candidate successor and is the publication
path. PID `176490` is the variable
detector raw/mask common-FAR successor. PID `177139` binds its future result to deterministic
automatic-mask evidence. PID `184956` is the corrected full-100 DINGO/AMPLFI successor; unlike the
older stale queue, it points to the active five-seed path and waits until scaling/OOD is finished so
that it does not contend for GPU 0. The score-blind O3b metadata task has now frozen 32
source-disjoint pairs for both H1+V1 and L1+V1. PID `188836` is downloading the first two-pair
H1+V1 shard for numeric detector validation; the first approximately 121 MB H1 source is making
steady ranged-download progress. PID `190048` is a no-destructive-restart repair successor pinned
to the hardened eviction implementation. PID `189637` and final retry PID `190128` guard the
AMPLFI capacity extension behind detector acquisition. PID `190936` waits to perform real
PyCBC/LAL H1/L1/V1 signal projection after the detector data receipt exists. No existing
scientific process has been stopped or modified.
The O4b/GWTC-5 access-log path is absent.

The earlier three-person human-mask gate has been retired before O4b access. Reviewer disagreement
showed that a unique real-glitch pixel boundary was not a stable target. Existing annotations are
retained as historical diagnostics but are not merged, used for training or treated as ground
truth. The primary path now uses deterministic component-derived masks and functional fixed-FAR/PE
evidence.

## Workstream registry

| Workstream | Completion | Current evidence | Remaining hard gate | Estimate after prerequisites |
|---|---:|---|---|---:|
| Reproducible code, CLI, provenance and CPU tests | 99.5% | Stable CLI plus tested detector-stratified streaming/sealing/merge chain | Final publication exports and any result-driven fixes | 1–3 engineering days |
| Physical multi-Q/multi-IFO corpus | 95% | Source-safe aligned corpus exists; 80-pair AMPLFI stream is complete but 2.30% below its train-duration gate | Add a small source-disjoint AMPLFI extension and pass capacity | 2–12 hours, network dependent |
| Automatic mask policy | 85% execution / 100% protocol | 100 validation scenes rematerialized; source components and masks replay exactly | Bind passing raw/mask continuous endpoint | 1–3 days, upstream dependent |
| Five-seed model selection | 15% execution / 100% protocol | Jobs are queued behind disk/acquisition | At least 4/5 passing seeds and frozen clean/glitch/family gates | 8–30 GPU hours |
| Fixed-epoch/fixed-update data scaling | 10% execution / 100% protocol | 250/500/1k and hard-subset controls are queued | Material improvement on both controls before scaling farther | 1–3 days |
| Continuous-background FAR/IFAR/`<VT>` | 15% execution / 100% locked-reduction infrastructure | Expanded variable-detector validation and locked shared-schedule reducers are queued | ≥23.02585093 post-DQ equivalent years, ≥3,000 DQ-eligible injections | 3–14 days |
| Raw versus mask search | 14% execution / 100% infrastructure | Variable-detector common-FAR successor and automatic-mask binder are queued | Same background/injections, frozen thresholds, clean non-inferiority | 1–3 days after model freeze |
| Detector-set/O4 OOD transfer | 12% execution / 98% infrastructure | Abstention/OOD protocol is frozen | Legitimate labeled/audited O4 sources, ≥500 locked rows | 1–4 days |
| Calibration and missing-IFO robustness | 24% execution / 100% infrastructure | Exact-subset audit, disjoint metadata planner and streaming numeric acquisition chain pass | Add 11 H1+V1 and 21 L1+V1 independent blocks, then materialize projected signals | 1–4 days |
| DINGO/AMPLFI paired PE | 27% evidence / 100% protocol | Backends, fair within-backend portfolio and corrected full-100 queue are present | ≥100 identical DQ-eligible BBH over ≥25 GPS blocks | 1–4 days after inputs exist |
| Validation publication ledger | 30% (3/10 verified) | Source-safe corpus, independent endpoint and unopened corpus replay pass | Seven immutable evidence items | 2–10 days; detector-stratified data now explicit |
| Locked O4b/GWTC-5 final ledger | 0% (0/9) | Correctly unopened | Validation 10/10, then one-time execution | 1–3 weeks after unlock |
| Paper-ready evidence and manuscript | ~15% | Methods/governance are strong; primary result is absent | Positive or retained null results, uncertainty, ablations, tables | 3–8 weeks |

These estimates are ranges, not guarantees. GWOSC throughput, DQ attrition, backend runtime and
negative promotion gates can extend them. A failed scientific gate is retained
as a result; it is not silently bypassed to preserve the schedule.

## New locked streaming and PE retention work

The streaming shard contract now publishes every raw and mask candidate instance without
threshold filtering, retains explicit null outcomes, validates OOD numeric sources, and merges all
shards into the predeclared suite inputs. Post-DQ injection weights are copied into both search arms
from one shared physical denominator.

A critical PE source-lifetime gap is now closed before source HDF5 eviction. The pre-access plan
binds the passing validation PE promotion, the exact common BBH prior, H1/L1, all three PE
conditions, the selection seed and a score-independent GPS-block-first hash order. A read-only audit
of the real 4,860-row locked inventory found 492 prior/IFO-compatible BBH injections in 492 distinct
GPS blocks. The plan freezes the first 300 as a DQ reserve for a final minimum of 100. There is no
post-access replacement.

For every selected injection that passes DQ, shard publication must retain complete clean,
contaminated and mask-conditioned numeric strain arrays with matching injection, waveform,
GPS-block, IFO and SHA-256 identities. Missing one triplet member, a non-finite tensor, a changed
hash or an escaped path prevents publication and explicitly forbids source eviction. The final
all-shard merger requires the retained set to equal the exact intersection of the frozen 300-event
pool and post-DQ eligibility.

The full CPU suite, Ruff, all shell syntax checks and `git diff --check` pass on the current
detector-validation streaming implementation.

## Validation ledger and detector-stratified data audit

A direct replay with the current official validation protocol now passes **3/10** gates with zero
failures: source-safe corpus, independent validation endpoint and locked corpus unopened. The prior
2/10 status omitted the already valid unopened-corpus contract. Seven evidence items are still
pending; no pending item is counted as passed.

Commit `b195836` closes a critical ledger loophole. The ledger now rejects the old H1/L1
`gps_block_permutation` evidence and requires the H1/L1/V1
`variable_detector_set_block_permutation`, multiway dependence bootstrap and expanded-background
lineage. The raw/mask gate likewise requires detector-set dependence audits. Positive and negative
unit tests prove that the new artifacts pass and the old artifacts fail.

Commits `bb8453b`, `40a30f0` and `4d2d603` extend the same rule to calibration perturbations. All
seven physical time-domain perturbations must be evaluated without threshold refitting on
H1+L1, H1+V1, L1+V1 and H1+L1+V1. Each exact detector stratum requires at least 25 injections.
The runner checks this before GPU work, and the aggregate and publication ledger repeat the gate.

The physical count audit explains why this is a real data task rather than a schema change:

- the current independent 3,000-injection endpoint is 3,000 H1+L1 and zero in the other three
  detector subsets;
- after enforcing one canonical row per independent network GPS block, the source-safe validation
  bank has 102 H1+L1, 14 H1+V1, 4 L1+V1 and 95 H1+L1+V1 blocks;
- the corresponding strict training counts are 560 H1+L1, 82 H1+V1, 35 L1+V1 and 425
  H1+L1+V1 blocks.

Therefore the next corpus expansion must add at least 11 H1+V1 and 21 L1+V1 independent,
validation-only physical contexts, and should target substantially more than the hard floor. The V1
channel must contain an actual antenna-projected waveform, not a zero-filled validity placeholder.
No train-row reassignment is allowed.

The new acquisition chain consumes only source-file/GPS-disjoint O3b plans and processes bounded
shards in this exact order: verified GWOSC download, catalog-event/DQ filtering, frozen
`hash_threshold_v1` assignment with zero test fraction, one numeric context per validation GPS
block, full artifact replay, and recoverable HDF eviction. Each shard receives an immutable receipt.
Only a cross-shard merge with zero repeated window/GPS identities and all four detector subsets at
or above 25 blocks can freeze the equal-count physical injection recipes. Candidate scores and test
strain are never opened by this path.

Commit `c5f5477` implements that shard seal/merge chain and is deployed at exact full commit
`c5f547732386a4f29b23dbff067bdef72857a1d8`. Commit `04b3072` adds the independent AMPLFI
capacity extension. It retains the original background seed and capacity policy, excludes both the
80-pair parent and its inherited 880-pair reservation, downloads at most eight new pairs in
two-pair shards, and stops at the first capacity pass after verified export and source eviction.
Both commits pass the full CPU suite, Ruff, all shell syntax checks and `git diff --check`.

Commit `14630d6` closes the DQ-zero-yield cleanup edge case. Its atomic eviction intent covers every
verified file in the downloaded shard, including a pair that contributes no validation block, and
can safely finish a partially completed deletion after interruption. Commit `b24ecd1` adds the
physical successor: external waveform equivalence, PyCBC/LAL detector projection, empirical
per-IFO SNR, geometric arrival annotation, and a final tensor-level audit requiring nonzero
analysis-window signal in every declared H1/L1/V1 channel. The expected frozen corpus is 400
injections, 100 per exact detector subset, with 300 containing a real V1 projection.

## Variable-detector locked search closure

Commit `b07787a` closes a previously hidden locked-evaluation failure: the suite declared H1+L1,
H1+V1, L1+V1 and H1+L1+V1, while the old reducer could only construct H1/L1 pairs and did not
create the four predeclared final search inputs. Injection ranking now evaluates every eligible
detector subset, applies every pairwise light-travel limit plus the frozen empirical timing
allowance, retains explicit missing-IFO semantics and prefers a larger coherent network only when
the ranking score ties.

The pre-access streaming plan now freezes a score-blind independent-offset schedule with H1 at
zero, L1 shifted forward and V1 shifted backward. Raw and mask arms replay the same schedule,
physical denominator and injection trigger set. Detector duty cycle is evaluated at each shifted
source window; all eligible subset channels are clustered jointly, so a single event cannot be
counted four times. The reduction fails closed unless every detector subset and source family is
covered and the background, injection, OOD and paired-PE minima all pass.

A read-only replay against the real 4,860-row O4b availability manifest selected 65 effective
offsets through slide index 65. It predicts 34.6749654 equivalent years, just above the
predeclared 1.5× safety target of 34.5387764 years. The schedule contains 177,421 H1+L1,
183,564 H1+V1, 186,970 L1+V1 and 140,401 H1+L1+V1 eligible shifted block instances. This is
pre-access capacity evidence only: the final reducer still requires at least 23.02585093 years
after DQ attrition and will not replace rejected blocks.

## Variable-detector validation identity closure

Commits `252fd06` and `124147f` close the validation/test candidate-identity mismatch. Validation
can now freeze score-blind independent circular block permutations with H1 fixed, L1 shifted
forward and V1 shifted backward, enumerate H1+L1, H1+V1, L1+V1 and H1+L1+V1, and cluster those
channels jointly. The threshold calibration replays the exact schedule and reconstructs union
exposure from detector availability rather than multiplying pair channels.

The new dependence audit assigns each exposure cell to as many as three physical source GPS
blocks plus its permutation offset. It reports a multiway source-block×offset pigeonhole bootstrap,
leave-one-block and leave-one-offset sensitivity, effective block/offset counts and exposure
dominance. A hand-calculated test reconstructs 40 seconds, five exceedances and
`FAR = 5 × 31557600 / 40`, and the full CPU suite passes.

The remote successor at commit `124147f1867ac217c7155addf7d6b384f65efb50` is deployed in a new
directory and queued as PID `174868`. A read-only audit found that the only completed legacy
candidate pipeline has 245.776 ms empirical timing uncertainty, so it is correctly rejected by the
frozen 10 ms network gate. The successor will only run on the pending five-seed-selected promoted
pipeline; it does not silently reuse the legacy timing result.

## Deterministic automatic-mask replay

Commit `290ef528a5871a3558b5f0b331aae960ec1e18b9` removed an unnecessary dependency on evicted
background strain when replaying a materialized injection signal. A fresh remote checkout then
rematerialized 100 validation overlaps from the original Gravity Spy numeric strain and isolated
injection components. The frozen set contains 100 unique injections, waveforms and glitches over
65 GPS blocks and 19 glitch labels; H1/L1 counts are 71/29.

The immutable audit
`/root/GW-YOLO-v2-artifacts/automatic-mask-publication-290ef52/automatic_mask_policy_audit.json`
passes with source-artifact hashes, component hashes, detector availability and the exact
`mixture = glitch + signal` identity replayed. Every glitch pseudo-mask is non-empty. Thirty-eight
chirp masks are empty because the frozen per-IFO SNR visibility policy suppresses sub-threshold
targets; they are retained as negative/null cases. The audit explicitly records
`human_ground_truth_claimed=false`, `pixel_accuracy_claim_allowed=false`, `test_rows_read=0` and
`scientific_claim_allowed=false`. It becomes validation-ledger evidence only after PID `168401`
binds it to a passing fixed-FAR raw/mask endpoint.

## Next execution order

1. Freeze the L1+V1 O3b source-disjoint metadata plan; run the bounded streaming acquisition until
   H1+V1 and L1+V1 both clear the 25-block floor, then freeze 100 physical recipes per subset.
2. Extend the AMPLFI background by the minimum predeclared source-disjoint increment needed to
   close the 4,592-second train deficit; do not alter the capacity policy.
3. Reclaim only verified/recoverable storage so the existing source-safe overlap job crosses the
   8 GiB guard; run one-seed, five-seed and
   fixed-control scaling gates.
4. Run PID `175740` on the five-seed-selected candidate artifacts and freeze an expanded,
   purpose-disjoint H1/L1/V1 threshold with the multiway dependence audit.
5. Run PID `176490`, then PID `177139`, to bind the completed automatic component-mask replay to
   the variable-detector functional raw/mask endpoint.
6. Materialize actual antenna-projected H1/L1/V1 signals for the frozen recipes and run the
   no-threshold-refit calibration/missing-IFO matrix.
7. Produce the validation PE portfolio, OOD, calibration and continuous-background artifacts and
   replay the validation ledger to 10/10.
8. Freeze the newest exact commit and the 300-event PE reserve. Only then create the one-time O4b
   access record.
9. Execute streaming raw/mask/OOD/PE retention, reduce FAR/IFAR/`<VT>`, run paired DINGO/AMPLFI
   backends, and finalize the nine locked endpoints.
10. Export positive, negative and null results into the paper registry; complete ablations,
   uncertainty tables and the PRD/JCAP/ApJS manuscript.
