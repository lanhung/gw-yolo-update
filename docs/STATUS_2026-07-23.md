# GW-YOLO research status — 2026-07-23 04:45 UTC

## Executive status

The project is approximately **35% complete against the publication objective**. This estimate
separates implementation from evidence: reproducible engineering is approximately **97%**, the
validation-to-locked pre-access protocol is approximately **98%**, but accepted paper evidence is
only approximately **15%**. The O4b/GWTC-5 locked-final ledger remains **0/9 by design**, because the
validation ledger is not yet 10/10 and the access log is still absent.

No claim of superiority to AMPLFI or DINGO is currently supported. The frozen comparison is
within-backend and paired across clean, contaminated and mask-conditioned inputs. The intended
distinct advantage remains mask-aware deglitching and search/PE robustness under overlapping
artifacts, not clean-data detection mAP or an absolute posterior-backend ranking.

## Live machine state

At 04:41 UTC the remote machine had 8.1 GiB free on the root volume and 3.6 GiB free on
`/root/autodl-tmp`. The external volume is below the frozen 8 GiB execution guard. The AMPLFI
background acquisition parent is alive; 18 shards have exported successfully and the active final
shard has verified 6/8 H1/L1 files. One H1 file is being resumed and one L1 file remains. GPU 0
(RTX 4090) is idle because the active critical path is network I/O and guarded disk reclamation, not
training compute.

The source-safe overlap, five-seed, scaling, continuous-background, raw/mask, calibration, OOD, PE
and validation-freeze successors remain alive and wait on immutable upstream reports. No existing
process has been stopped or modified. The O4b/GWTC-5 access-log path is absent.

The blinded human-mask services are healthy. Reviewer B has genuinely finalized all 93 frozen
tasks. Reviewers A and C have produced 0/93 each, so the scientifically valid total remains
93/279. Three distinct people are required; synthetic or duplicated reviewers are forbidden.

## Workstream registry

| Workstream | Completion | Current evidence | Remaining hard gate | Estimate after prerequisites |
|---|---:|---|---|---:|
| Reproducible code, CLI, provenance and CPU tests | 97% | Stable CLI, immutable reports, hashes, atomic writes and fail-closed tests | Finish streaming worker and endpoint reducers | 2–4 engineering days |
| Physical multi-Q/multi-IFO corpus | 92% | Source-safe aligned numeric corpus and immutable audits exist | Finish active acquisition/export and final combined audit | 1–6 hours, network dependent |
| Blinded human mask validation | 33.3% | 93/279 genuine annotations | Two independent people each finish 93 tasks | Human-dependent; automation then 2–6 hours |
| Five-seed model selection | 15% execution / 100% protocol | Jobs are queued behind disk/acquisition | At least 4/5 passing seeds and frozen clean/glitch/family gates | 8–30 GPU hours |
| Fixed-epoch/fixed-update data scaling | 10% execution / 100% protocol | 250/500/1k and hard-subset controls are queued | Material improvement on both controls before scaling farther | 1–3 days |
| Continuous-background FAR/IFAR/`<VT>` | 12% execution / 98% infrastructure | Dependence-aware background and hierarchical injection bootstrap exist | ≥23.02585093 equivalent years, ≥3,000 DQ-eligible injections | 3–14 days |
| Raw versus mask search | 12% execution / 99% infrastructure | Common-FAR paired comparison and null retention exist | Same background/injections, frozen thresholds, clean non-inferiority | 1–3 days after model freeze |
| Detector-set/O4 OOD transfer | 12% execution / 98% infrastructure | Abstention/OOD protocol is frozen | Legitimate labeled/audited O4 sources, ≥500 locked rows | 1–4 days |
| Calibration and missing-IFO robustness | 15% execution / 99% infrastructure | Required strata and fixed perturbations are predeclared | Complete all strata without retuning | 1–3 days |
| DINGO/AMPLFI paired PE | 25% evidence / 100% protocol | Backends and validation portfolio chain are present | ≥100 identical DQ-eligible BBH over ≥25 GPS blocks | 1–4 days after inputs exist |
| Validation publication ledger | 20% (2/10) | Independent validation endpoint and unopened corpus pass | Eight immutable evidence items | 2–7 days plus human annotation |
| Locked O4b/GWTC-5 final ledger | 0% (0/9) | Correctly unopened | Validation 10/10, then one-time execution | 1–3 weeks after unlock |
| Paper-ready evidence and manuscript | ~15% | Methods/governance are strong; primary result is absent | Positive or retained null results, uncertainty, ablations, tables | 3–8 weeks |

These estimates are ranges, not guarantees. GWOSC throughput, DQ attrition, backend runtime,
human annotation and negative promotion gates can extend them. A failed scientific gate is retained
as a result; it is not silently bypassed to preserve the schedule.

## New locked streaming and PE retention work

The streaming shard contract now publishes every raw and mask candidate instance without
threshold filtering, retains explicit null outcomes, validates OOD numeric sources, and merges all
shards into the predeclared suite inputs. Post-DQ injection weights are copied into both search arms
from one shared physical denominator.

A critical PE source-lifetime gap is now closed before source HDF5 eviction. The pre-access plan
binds the passing validation PE promotion, the exact common BBH prior, H1/L1, all three PE
conditions, the selection seed and a score-independent GPS-block-first hash order. A read-only audit
of the real 4,860-row locked inventory found 492 prior/IFO-compatible BBH injections in 492 distinct
GPS blocks. The plan freezes the first 300 as a DQ reserve for a final minimum of 100. There is no
post-access replacement.

For every selected injection that passes DQ, shard publication must retain complete clean,
contaminated and mask-conditioned numeric strain arrays with matching injection, waveform,
GPS-block, IFO and SHA-256 identities. Missing one triplet member, a non-finite tensor, a changed
hash or an escaped path prevents publication and explicitly forbids source eviction. The final
all-shard merger requires the retained set to equal the exact intersection of the frozen 300-event
pool and post-DQ eligibility.

The full CPU suite, Ruff, shell syntax checks and `git diff --check` pass at this snapshot.

## Next execution order

1. Finish the last two AMPLFI background files and export/evict their verified source cache.
2. Allow the existing source-safe overlap job to cross the 8 GiB guard; run one-seed, five-seed and
   fixed-control scaling gates.
3. Complete the two missing genuine human annotation manifests; automatically merge consensus and
   bind the raw/mask endpoint.
4. Produce the validation PE portfolio, OOD, calibration and continuous-background artifacts and
   replay the validation ledger to 10/10.
5. Freeze the newest exact commit and the 300-event PE reserve. Only then create the one-time O4b
   access record.
6. Execute streaming raw/mask/OOD/PE retention, reduce FAR/IFAR/`<VT>`, run paired DINGO/AMPLFI
   backends, and finalize the nine locked endpoints.
7. Export positive, negative and null results into the paper registry; complete ablations,
   uncertainty tables and the PRD/JCAP/ApJS manuscript.
