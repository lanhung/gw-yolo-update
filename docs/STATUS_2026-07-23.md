# GW-YOLO research status — 2026-07-23 03:10 UTC

## Executive status

The project is approximately **34% complete against the full publication objective**,
**94% complete in reusable engineering infrastructure**, and about **15% complete in
paper-grade scientific evidence**. These percentages are intentionally separated:
implemented code is not counted as a positive scientific result.

The independent aligned Gravity Spy acquisition has completed all 18/18 shards. The
final source-component-safe split contains 3,016 physical network samples: 2,449 train
and 567 validation. It covers 21 glitch families and 1,317 split-disjoint network GPS
blocks. The final audit reports zero shared glitch IDs, network GPS blocks, numeric
sample hashes or source HDF5 URLs across train and validation.

The first post-acquisition model-selection attempt failed before training because the
old overlap producer requested 2,449 pairs, while only 1,170 had a physically supported
shared detector set in the current clean injection bank. No result was promoted. The
pairing implementation now uses an exact maximum-cardinality flow over detector-set
categories and reports every unpaired physical row. The immediate reproducible scale
is therefore 1,170 H1+L1 train overlaps and 210 H1+L1 validation overlaps; the
predeclared 250/500/1,000 scaling curve remains feasible. A V1-aware clean injection
bank is required before the H1+L1+V1, H1+V1 and L1+V1 glitch rows can all contribute to
physical chirp+glitch training.

The O4b/GWTC-5 corpus remains unopened. A new pre-access execution freeze now binds all
4,860 score-blind injection attempts to an immutable streaming order, source URLs,
work directories, storage floor and no-replacement/no-result-dependent-stopping policy.
The one-time access command now refuses to create the irreversible access log unless
that execution plan and all 11 frozen model/threshold/OOD/PE/catalog dependencies replay
by hash. Actual O4b strain production will still occur only after validation reaches
10/10.

## Current workstream ledger

Percentages are local to each workstream. “Queued” means an exact fail-closed command
exists; it does not mean its scientific endpoint passed.

| Workstream | Completion | Current evidence | Next gate and estimated time |
|---|---:|---|---|
| Reproducible code/provenance | 99% | Stable CLI, atomic JSON, commit/config/manifest/model hashes, exact commands and CPU tests | Final backend/runtime replays; 1–2 days interleaved |
| Independent Gravity Spy acquisition | 100% | 18/18 shards complete; every requested row is usable or explicitly rejected | Closed |
| Final group-safe corpus | 100% | 3,016 rows; train 2,449, val 567; zero overlap in four physical/source fields | Closed for this corpus version |
| Detector-compatible overlap materialization | 15% | Old attempt failed at the true 1,170/2,449 train and 210/567 validation capacity boundaries; exact max-flow fix is tested, pushed and queued at commit `65e479f` | Materialize after the active AMPLFI acquisition releases the storage guard; 4–10 hours |
| Five-seed model selection | 5% | No seed result accepted; all downstream jobs stopped after the capacity failure | One-seed two-arm gate, then 4 more seeds; 18–36 hours after materialization |
| Fixed-epoch/fixed-update scaling | 5% | 250/500/1,000 protocol and hard endpoint exist; no new curve result | Run both controls across five seeds; 2–4 days |
| V1-aware training-bank expansion | 10% | Need is now measured: 1,279 train rows contain V1 and are not compatible with the current injection bank | Generate group-safe variable-IFO injections only if hard endpoint justifies scale; 2–5 days |
| Continuous background FAR/IFAR/`<VT>` | 10% evidence / 97% infrastructure | Corrected 880-pair O4a schedule, clustered bootstrap and hierarchical injection bootstrap exist | Execute promoted model to ≥23.02585093 equivalent years; 2–5 days |
| Raw vs mask-conditioned search | 10% evidence / 99% infrastructure | Identical-background/injection comparison and clean non-inferiority gates exist | Complete promoted-model streaming plus human consensus; 2–5 compute days after model |
| Human mask validation | 33.33% annotations | Reviewer-b: 93/93; reviewers a/c: 0/93; total 93/279 | Two genuine people must finish 93 masks each; typically 2–4 hours/person, calendar time unknown |
| Detector-set OOD/run transfer | 10% evidence / 98% infrastructure | Frozen abstention/energy protocol and family-safe successor exist | Run only after five-seed promotion; 6–12 hours |
| Calibration perturbation robustness | 12% evidence / 99% infrastructure | Seven score-blind scenarios frozen | Run after continuous calibration; 6–18 hours |
| DINGO within-backend paired PE | 50% | Official O4a runtime/model smoke passed; final-split V3 batch is waiting | 100-event raw/contaminated/mask batch; 1–3 days |
| AMPLFI within-backend paired PE | 22% | v0.6 environment, data bridge and stage-1 training chain exist | Finish validation-selected model and identical-event batch; 2–4 days |
| Absolute DINGO/AMPLFI ranking | 0% evidence / 100% gate | Current native priors/waveforms are not equivalent, so absolute ranking is forbidden | Train truly matched models or retain within-backend deltas |
| Validation publication ledger | 20% (2/10) | Independent O4a endpoint and unopened GWTC-5 contract pass | Eight evidence items remain; earliest compute-only completion 4–8 days |
| Locked O4b/GWTC-5 protocol | 95% pre-access / 0% final evidence | 4,860 frozen attempts; streaming execution freeze added; access log absent | Implement post-access shard reducer, then one-time run after 10/10; 1–3 weeks dominated by transfer |
| Locked-final ledger | 0% (0/9) | Intentionally no O4b event-level strain or score opened | One-time suite only after validation freeze |
| Paper-ready result package | about 15% | Negative/diagnostic evidence retained; primary fixed-FAR claim absent | Complete search, robustness, PE and locked transfer evidence |

## Final physical data accounting

The completed acquisition requested 2,009 new physical rows: 1,653 train and 356
validation. It produced 1,410 usable aligned numeric samples and 599 explicit rejects:
1,191/462 for train and 219/137 for validation. Combined with the previously verified
bank, the source-component-safe corpus contains:

| Quantity | Train | Validation | Total |
|---|---:|---:|---:|
| Physical numeric samples | 2,449 | 567 | 3,016 |
| Unique glitches | 2,449 | 567 | 3,016 |
| Unique network GPS blocks | 1,102 | 215 | 1,317 |
| O1 rows | 476 | 78 | 554 |
| O2 rows | 449 | 22 | 471 |
| O3a rows | 588 | 337 | 925 |
| O3b rows | 936 | 130 | 1,066 |
| H1+L1 rows | 1,170 | 210 | 1,380 |
| H1+L1+V1 rows | 1,036 | 219 | 1,255 |
| H1+V1 rows | 201 | 134 | 335 |
| L1+V1 rows | 42 | 4 | 46 |

All rows are numeric multi-Q tensors derived from strain, not rendered images. The
primary representation has three detector slots and Q values 4, 8 and 16. Missing
detectors retain an explicit availability mask. Current glitch masks remain weak
metadata-derived masks and cannot support a publication mask claim until the three-human
consensus endpoint completes.

The final split has no unrepresented glitch family. The rarest family is Helix with
4 train and 5 validation rows. This is sufficient for the frozen family-floor audit,
but not for a strong class-specific accuracy claim; it will be treated as a hard/OOD
stratum rather than oversampled image augmentation.

## Training-data conclusion

The earlier concern that training data was too small was correct, but “more rows” is
not the whole answer:

1. The physical glitch corpus has grown from the initial bank to 3,016 independent
   numeric network samples and now supports a real source-safe split.
2. The current clean injection bank, not the glitch count, is the immediate overlap
   bottleneck. It supports only 1,170 strict H1+L1 train pairs.
3. Scaling beyond 1,000 should not start by schedule. First compare 250/500/1,000 under
   both fixed-epoch and fixed-update controls on the frozen hard O4a endpoint.
4. If the endpoint improves materially, the next data investment should be a
   variable-IFO H1/L1/V1 injection bank and later-run/OOD glitch coverage, not duplicated
   rendered images or mosaic/mixup.
5. If the endpoint saturates, the limitation is more likely representation, calibration
   or domain transfer; increasing nominal image count would not create a phase change.

## Critical path and forecast

The earliest credible compute-only path to a 10/10 validation freeze is approximately
4–8 days, assuming the one-seed gate passes and the remote service remains available.
The human-mask item has no honest automatic ETA because two additional genuine people
must annotate; with reviewers available, the work itself is typically one day.

After validation 10/10, the one-time locked suite is expected to take roughly 1–3 weeks.
The uncertainty is dominated by sequential O4b source transfer and post-access streaming,
not GPU inference. A paper-grade analysis and manuscript would then require another
2–4 weeks for five-seed tables, uncertainty figures, ablations, negative-result retention
and internal review. A realistic end-to-end forecast is therefore **4–8 weeks**, not an
unqualified promise of a target accuracy.

The result target remains unchanged: demonstrate at common FAR that explicit masks and
network coherence recover sensitivity or downstream posterior reliability under
nonstationary/overlapping artifacts, while preserving clean-data non-inferiority.
DINGO and AMPLFI remain downstream PE backends; the defensible comparison is their paired
raw/contaminated/mask-conditioned change under identical events, not detection mAP.

## Immediate ordered actions

1. Push and deploy the maximum-cardinality detector-set pairing fix.
2. Rebuild immutable train/validation overlaps and verify the joint leakage audit.
3. Run the one-seed uniform versus family-balanced gate.
4. If it passes, complete five seeds; otherwise retain the negative result and revise
   the representation without touching O4b.
5. Run 250/500/1,000 fixed-epoch/fixed-update scaling, hard endpoint, OOD and calibration.
6. Complete family-safe continuous-background FAR/IFAR/`<VT>` and raw/mask comparison.
7. Complete final-split V3 DINGO/AMPLFI paired PE.
8. Obtain two additional human reviewers and bind consensus.
9. Freeze validation 10/10 plus the exact 4,860-row streaming execution plan.
10. Only then write the one-time O4b access log and execute all nine locked endpoints.
