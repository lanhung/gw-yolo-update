# GW-YOLO research status — 2026-07-24 05:23 UTC

## Executive status

The project is approximately **47% complete against the publication objective**. This estimate
separates implementation from evidence: reproducible engineering is approximately **99.9%**, the
validation-to-locked pre-access protocol is approximately **99.9%**, but accepted paper evidence is
only approximately **22%**. The increase is due to the passing score-blind O4a acquisition pilot,
completed hash-bound detector-stratified
materialization and AMPLFI training capacity, not to a positive model or search result. The
O4b/GWTC-5 locked-final ledger
remains **0/9 by design**, because the
validation ledger is not yet 10/10 and the access log is still absent.

No claim of superiority to AMPLFI or DINGO is currently supported. The frozen comparison is
within-backend and paired across clean, contaminated and mask-conditioned inputs. The intended
distinct advantage remains mask-aware deglitching and search/PE robustness under overlapping
artifacts, not clean-data detection mAP or an absolute posterior-backend ranking.

## 05:23 UTC incremental execution snapshot

Commit `6cd6ac7aa69508d26643f6d081135565d21b1e37` implements the physically projected
H1/L1/V1 signal-bank arm. It regenerates all detector responses in one PyCBC/LALSimulation call,
checks the regenerated H1/L1 arrays against the existing source artifact, and computes
detector-complete training SNRs from frozen O4 reference PSDs
(`aLIGOAdVO4T1800545` for H1/L1 and `AdVO4T1800545` for V1). A real one-event smoke passed with
minimum H1/L1 normalized overlap 0.9999999794 and maximum relative L2 error
0.00020310. The full 10,000-waveform run is active as low-priority PID `304305`; it reached
900/10,000 in its first three minutes.

This product is explicitly labeled `robustness_ablation_not_sample_scaling`. Commit
`42134d05e0c6bc49a175dc61d9f949dc4ec22285` makes the capacity gate reject a detector-expanded
manifest unless the expansion report binds its exact SHA-256, declares zero test access and
forbids a same-distribution scaling claim. PID `305027` waits for the 10k report and will compute
the expected 1,170 H1+L1 same-distribution versus 2,449 all-detector-set capacities. It cannot
authorize training before the frozen hard endpoint and cannot relabel the V1 arm as more H1+L1
data.

The corrected validation-loss head is at epoch 8/20 with validation loss 3.22034 and clean-chirp
retention 1.000; fixed-threshold glitch IoU remains zero. AMPLFI Stage 1 is at epoch 75 and more
than 15,099/20,000 updates. O4a acquisition has cached approximately 2.0 GiB. The three original
low/mid/high acquisition processes remain active and untouched; consolidated recovery PID
`197423` will begin only after all three exit naturally.

## 05:02 UTC incremental execution snapshot

The corrected validation-loss glitch head is at epoch 4/20 (588 optimizer updates). Validation
loss improved from 3.44521 at epoch 3 to 3.40081 at epoch 4 and clean-chirp retention remains
1.000, but fixed-threshold glitch IoU is still zero. It is therefore active optimization evidence,
not a promoted mask result. At the observed cadence it needs approximately two more hours before
the residual-adapter and conditional scaling chain can make its validation-only decisions.

AMPLFI Stage 1 is at epoch 68/100 and more than 13,649/20,000 configured updates. The last complete
epoch-67 validation loss is 4.33440. Its validation-selected checkpoint, posterior coverage,
bias/width and paired contaminated/mask-conditioned endpoint remain pending; no backend
comparison claim is allowed yet.

The score-blind O4a cache has grown to approximately 1.8 GiB and the data node retains
approximately 2.4 TiB free. The low-range shard 1 and mid-range shard 86 are actively resuming.
The high-range shard 172 wrapper remains active after two bounded download attempts. GWOSC range
requests are currently network-limited: individual 900-second attempts preserve partial bytes
but may exit before a full HDF5 file completes. The three existing jobs are left untouched; any
recovery must begin only after they exit and must reuse the immutable cache.

Commit `6690258e75e724b91c39e050cf31ae1f7f82e5f0` was replayed against the real manifests. The
result at
`/root/GW-YOLO-v2-artifacts/overlap-expansion-capacity-6690258/physical_overlap_expansion_capacity.json`
has SHA-256 `5599ab2a51f36c46581597db2d859cf37c608348602442531694bba7c6e8d4df`.
It reads no test rows and exactly reproduces 1,170 current, 1,170 same-distribution and 1,170
all-current-detector-compatible physical groups. Commit
`588e9ea0f3267f86e9e4092853ff0ca5546fcd01` adds the fail-closed replay queue; remote PID
`301060` waits for the frozen validation hard endpoint and will either bind a capacity report or
write an explicit upstream-incomplete receipt. It never launches training.

## 04:30 UTC incremental execution snapshot

The family-balanced 51-parameter head completed 20/20 epochs with clean-chirp retention 1.000,
but fixed-threshold glitch IoU remained zero. Its validation loss decreased monotonically from
3.53333 to 2.65756. This is evidence of a head-capacity/decision-boundary failure, not a passing
mask result and not evidence that simply adding more physical rows will help. The process is
finishing its report; the validation-loss selector, residual adapter, five-seed adapter gate and
conditional two-control scaling successor remain serialized behind it.

AMPLFI Stage 1 has entered epoch 55/99 and passed 11,000 of 20,000 configured optimizer updates.
Recent epoch-level validation loss is approximately 4.66, compared with 9.83 after epoch 0. This
is optimization progress only. Validation-selected checkpoint loading, posterior coverage,
bias/width, sample efficiency and contaminated/mask-conditioned robustness have not yet run.

Commit `b99619d` removes the remaining machine-path ambiguity in the paired-PE inputs themselves.
The clean, contaminated and mask-conditioned arrays, common prior, mask model/policy, DINGO and
AMPLFI native conditioning artifacts/configurations, and frozen validation receipts are now
exported into a SHA-256-addressed, deduplicated bundle. Import verifies every byte, rejects
backend event drift, rewrites absolute paths and rebuilds the exact report/manifest layout
consumed by both backends. Cross-machine evidence bundles were already portable at `6070c9a`;
the input and output sides are now both relocatable. The paired-PE selector also fails closed when
its five seed reports span training commits unless the exact AST/whole-file compatibility audit
passes; that audit is copied into the portable input bundle and remains part of the downstream
provenance graph.

The output bundle schema is now versioned and self-contained at the PE-model level as well as the
event level. When a backend summary contains `model_metadata`, export verifies and packages the
validation-selected model/checkpoint, native prior, analysis prior, conditioning/training
configuration, selection report and every other hash-bound metadata artifact. Import projects
those paths and rewrites the summary to the projected metadata. Training strain banks are not
recursively copied: their frozen manifests are retained, avoiding an unbounded transport while
preserving the exact sample identity.

The next queued handoff is validation-only and fail-closed. A positive residual-adapter five-seed
summary will materialize up to 100 paired injections spanning at least 25 GPS blocks, export the
shared input bundle, run DINGO and AMPLFI on that same content identity, return both portable
within-backend bundles and evaluate matched-event within-backend deltas with 10,000 hierarchical
bootstrap replicates. A negative adapter decision writes a terminal negative receipt and performs
none of those expensive jobs. Absolute DINGO/AMPLFI ranking remains forbidden.

The port-2711 credentials resolve to the already active O4a data node, not a new independent
worker. The container still returns `NVML: Unknown Error`; no CUDA capacity is counted and no
driver/process was modified. It has approximately 2.4 TiB free and continues two score-blind,
test-fraction-zero download ranges. Shard 0 is complete, shard 1 is active, the high-range shard
172 prefetch is active, and the cache is approximately 1.3 GiB. The full acquisition remains
network-bound. A third score-blind range now prefetches shards 86--171 in a separate output
namespace (PID `196617`), disjoint from the existing high-range 172--219 prefetch; all ranges share
the immutable content cache, so the serial low-range acquisition can later verify and reuse bytes
without downloading them again.

The training-volume ceiling is now quantified rather than inferred from rendered rows. The
current overlap train manifest contains 1,170 unique mixtures, waveforms, injections and glitches
across 560 glitch GPS blocks; every row is H1+L1. The group-safe Gravity Spy train corpus contains
2,449 unique glitches, but its detector-set composition is 1,170 H1+L1, 1,036 H1+L1+V1,
201 H1+V1 and 42 L1+V1. The 10,000-waveform clean signal bank currently supports H1+L1 only, so
the exact maximum-cardinality compatible pairing is 1,170. There is no unused same-distribution
row hidden in the existing inputs. Reaching a materially larger same-distribution endpoint
therefore requires new unique H1+L1 glitches/GPS blocks (the O4a mining path), while enabling the
1,279 V1-containing glitches requires a new physically projected multi-IFO signal bank and must
be reported as a separate detector-set robustness ablation.

The new `physical-overlap-expansion-capacity` gate makes this distinction executable. It binds an
authorized hard-endpoint decision to current/candidate manifest hashes, computes exact
maximum-cardinality detector-compatible capacity, and authorizes next-scale training only when
the same detector-set distribution has enough independent physical groups. A capacity increase
caused only by adding detector subsets is explicitly rejected as same-distribution data scaling.

## 04:07 UTC incremental execution snapshot

The family-balanced 51-parameter glitch-head arm has completed 15/20 epochs. Its epoch-15
validation loss is 2.89506 and clean-chirp retention remains exactly 1.000. It remains an active
validation run, not a promoted model. The corrected validation-loss head and conditional
16-channel residual adapter remain serially queued behind it, so no GPU contention has been
introduced.

Commit `e386fad` closes the adapter-successor gap. A positive one-seed adapter must now pass the
same absolute clean-retention, glitch-IoU, family-median and zero-family validation gates before
four additional seeds can run. Five-seed expansion then requires at least 4/5 passing seeds and
the frozen median gates. Only a passing five-seed summary authorizes the 250/500/1,000/full
physical-data curve under both fixed-epoch and exactly 4,000-update controls. The adapter scaling
configs preserve the current 16-channel architecture, optimizer and uniform physical-row
sampling policy; they do not confound data volume with a new family sampler. GPU-idle checks for
scaling and hard-endpoint work are now scoped to the assigned physical device. All negative
one-seed and five-seed outcomes receive explicit receipts and perform no scaling.

Commits `05274c6`, `0e41fca` and `043c699` close the downstream adapter handoff. A single
hash-replaying resolver now supports uniform, family-balanced and glitch-adapter summaries across
candidate calibration, continuous background, mask, numeric raw/mask and paired-PE consumers. A
positive adapter summary can emit its own five-seed gate receipt before the longer scaling matrix
finishes. Because seed 20260720 was produced at `cf877fc` while the queued follow-up seeds use
`790e9dc`, a code-level compatibility audit was required rather than assumed. The audit passes:
the frozen adapter config, `numeric.py`, `physical_training.py`, I/O/runtime modules, and AST
semantics of all 18 training functions/classes are identical across the two commits. Mixed
training commits now fail closed without this audit. PID `290028` waits for a positive adapter
five-seed summary before running the detector-stratified candidate/calibration path; PID `291792`
does the same for the six-arm mask/timing and numeric raw/mask path. Both exit without work on a
negative summary.

The AMPLFI Stage-1 worker has entered epoch 46/99 and passed 9,200/20,000 configured updates. Its
latest observed validation loss is approximately 4.80, versus 9.83 after epoch 0. This is
optimization progress only; posterior accuracy, coverage and robustness remain unevaluated.
Commit `6070c9a` is deployed on both PE machines and provides content-addressed export/import of
within-backend DINGO and AMPLFI evidence, including every referenced posterior and conditioning
artifact, so machine-local absolute paths no longer prevent the final paired portfolio.

The O4a score-blind pilot has now passed. Its
`verified_development_strain_batch` receipt binds the unchanged plan SHA-256
`615c0293c97d9b6900fadb4487c1cde35f4f4015c0f924cac7535e598b5a3a82`, reports four selected
aligned pairs and verifies all eight HDF5 files. The full 880-pair low-range acquisition therefore
crossed its exact gate: shard 0 reused and reverified the pilot files and is complete, while shard
1 is downloading. The disjoint high-range prefetch independently crossed the same gate and has
begun shard 172. The shared cache is approximately 1.0 GiB. Both paths remain score-blind O4a
development acquisition with test
fraction zero; no model or candidate score is loaded. O4b/GWTC-5 remains untouched.

## 03:18 UTC execution, data-capacity and compute snapshot

Both earlier trainable-backbone overlap strategies are now retained negative results. The primary
full-model arm completed 20 epochs with maximum clean-chirp retention 0.51977. The
teacher-anchored arm also completed 20 epochs and improved the maximum retention to 0.86454, but
neither reached the frozen 0.95 non-inferiority gate. No checkpoint from either arm is eligible.
These failures argue against scaling the same optimization recipe: the immediate limitation is
interference/representation, not demonstrated shortage of rendered examples.

Commit `cb0277f` adds the predeclared chirp-preserving fallback. It freezes the backbone and all
chirp output rows, trains only glitch rows, disables optimizer weight decay in this mode and
verifies the frozen tensors bit-for-bit in the selected checkpoint. Its authorization is
validation-only and requires the completed negative teacher result. The uniform seed-20260720 arm
completed 20/20 epochs. Clean-chirp IoU retention remained structurally unchanged at 1.000,
training loss decreased from 3.62023 to 2.73653 and validation loss decreased from 3.53407 to
2.66219. The original fixed-threshold selector nevertheless retained epoch 1 because every
fixed-0.5 diagnostic glitch IoU tied at zero. Its frozen validation threshold sweep then found
zero glitch IoU at every predeclared threshold from 0.2 through 0.9. The result is retained as a
negative head-capacity result, not repaired by an ad hoc lower threshold. The family-balanced arm
is active and has completed 7/20 epochs with clean retention 1.000. Its epoch-7 training and
validation losses are 3.31328 and 3.26626, while fixed-0.5 glitch IoU remains zero. If the
one-seed decision passes, the same chain runs the predeclared five seeds. Detector-set baseline, calibration
robustness and scaling/OOD successors are queued behind that gate and fail closed if the
five-seed summary is absent.

Commit `811082c` adds a fail-closed successor for the threshold-aliasing case exposed by this
history. When all fixed-0.5 glitch IoUs are equal, retaining epoch 1 can discard later loss
improvement. The successor therefore selects only glitch-head checkpoints by validation loss;
this is threshold-independent because the chirp head and backbone are bit-exact constants. Final
mask thresholds are still frozen on validation with the unchanged predeclared grid. PID `259887`
waits for the current chain and exits without work if that chain produces a positive five-seed
summary; otherwise it runs the corrected selection in a new output root. The current run remains
an immutable result and no test rows were used to design the successor.

The negative uniform result also exposes a representation limit: the head-only scope has only 51
effective trainable parameters. Commits `0531bed` and `cf877fc` therefore add a strictly
conditional zero-initialized residual glitch adapter. The detector-set backbone and complete
base head remain frozen; the new decoder can alter only glitch logits, and its initial forward
pass is bit-exact with the pretrained model. The fallback uses validation-loss checkpoint
selection, the unchanged 0.2--0.9 threshold grid, 16 adapter channels, 20 epochs and one assigned
GPU. Twenty-eight real-Torch targeted tests on the main server pass, including checkpoint reload
and bit-exact chirp-output preservation. PID `280584` waits for the corrected head chain and runs
the adapter only if both corrected head arms produce a hash-bound negative receipt; a positive
head result makes it exit without training.

The detector-stratified physical validation corpus is complete at commit `8d67203`: 400
validation-only injections, 100 for each of H1+L1, H1+V1, L1+V1 and H1+L1+V1; 300 include V1.
The population contains 180 BBH, 120 BNS and 100 NSBH injections. Mixed source-bank contexts are
handled by a hash-bound, centered crop to the common 8-second context around the 4-second analysis
window. The audit passes, reads zero test rows and preserves 400 unique injection/waveform IDs.
The corresponding background stream contains 102, 42, 30 and 95 source blocks for those detector
subsets respectively. This closes the earlier H1+L1-only calibration-corpus deficiency, but is
still validation evidence and not a final 0.1/year search background.

AMPLFI training capacity is also complete. A two-pair source-disjoint extension increased the
frozen bank from 195,408 to 202,448 train seconds while retaining 56,520 validation seconds,
819 train GPS blocks and 232 validation blocks. Cross-split overlap and test-strain reads are zero.
The no-copy bank contains 822 train and 232 validation files (approximately 6.6 GiB through
symbolic links). A real five-epoch/250-update engineering smoke on the auxiliary GPU exposed that
Zuko's CUDA `cumsum` has no strict deterministic implementation under Torch 2.5.1. The failed
strict run is retained. Commit `29c306f` switches Lightning to seeded `deterministic: warn`, so
unsupported operations emit explicit warnings rather than aborting. The corrected smoke completed
all 250 updates, reduced validation loss from 10.05629 to 9.05758 and reloaded the
16,962,024-parameter checkpoint successfully. It is explicitly not a publication candidate.
Commit `ffb5630` scopes the idle-GPU gate to an explicitly assigned device. The corrected
100-epoch/20,000-update/5.12-million-example stage-1 run is now active on auxiliary GPU 3 as PID
`2849142`. It has completed epochs 0--28; validation loss decreased from 9.82877 after epoch 0 to
5.27559 after epoch 28. No AMPLFI accuracy or posterior claim exists until validation-only
checkpoint selection and the paired clean/contaminated/mask-conditioned posterior ladder pass.

The first auxiliary server (`gpu.chzmark.com:2338`) has four RTX 5000 Ada GPUs. GPUs 0--2 are
occupied by unrelated work; GPU 3 is reserved for the active AMPLFI run. A fully isolated Python
3.12 environment passes
`pip check`, AMPLFI 0.6.0/Torch 2.5.1+cu124 import, one-device CUDA tensor execution and the
AMPLFI CLI smoke test. Its sorted 191-package freeze has SHA-256
`830dbba1ab9ce16cfa31b695b39820c7429f5cbb7709f7156a76bef6c42016dd`.
The exact AMPLFI bank was copied into a new `/root/autodl-tmp/GW-YOLO-v2-*` namespace and all
1,054 files (7,001,482,575 bytes; 822 train and 232 validation) replayed against their source
SHA-256 values. All 1,054 symbolic links resolve, and test reads remain zero. The auxiliary
server is therefore a verified training/PE worker. Its active stage-1 output uses a new commit and
output root; it does not overwrite the old main-server strict-deterministic queue, which is
expected to fail closed if it eventually reaches its sanity check. The newly supplied server
on port 2711 is reachable and has 2.4 TiB free, but its current container exposes only one device
node, NVML returns `Unknown Error`, and PyTorch reports `CUDA available=False` with zero devices.
It is therefore not counted as three-GPU capacity and no driver or external process was modified.
Instead, exact commit `3fb465c` and the authorized, score-blind 880-pair O4a validation plan were
deployed there as a CPU/data worker. The parent plan SHA-256 is
`20cf0cf3e66baa19e0a5439c2a11cd71a89f5dcc88ea88e68f9631b86fac4430`;
it remains unlocked O4a development data with candidate scores uninspected. A four-pair/eight-file
pilot is active against shard 0. Its frozen shard SHA-256 is
`615c0293c97d9b6900fadb4487c1cde35f4f4015c0f924cac7535e598b5a3a82` and the eight
GWOSC objects total 833,075,642 bytes (0.776 GiB). The pilot performs resumable range downloads,
SHA-256 hashing and full HDF5 strain/quality-bit verification only; it does not load a model or
produce candidate scores. Five files have passed. One 16.1 MB range reached only 9.4 MB before
the immutable 900-second retry window and correctly failed without a batch receipt. PID `194840`
resumed the same range cache and run identity; the second bounded attempt brought seven of the
fifth file's eight ranges to completion and left the last approximately 82 KiB short. The third
attempt completed that file and PID `194960` is now fetching the sixth using the same-layout
bounded retries. No completed byte was discarded and no range or verification limit was relaxed.
PID `194842` waits for the exact eight-file pilot report, then
and only then starts the 880-pair/220-shard `hash_threshold_v1`, test-fraction-zero acquisition.
PID `195031` independently waits for that same report before downloading only authorized shards
172--219 as a score-blind high-range prefetch. Commits `3f886fb` and `f9f56a6` add a second
bounded recovery tier that preserves the eight-worker range partition and separately records the
original pilot CLI commit and recovery-wrapper commit. PID `195427` remains dormant until the
first retry supervisor exits; it produces no recovery receipt and no duplicate handoff if the
authoritative path succeeds. PIDs `195445` and `195446` can start the full range and prefetch only
when that recovery receipt exists.

The independent PE corpus transfer is also resumable and audited. Its source contains 3,235
ordinary files and no symbolic links. The frozen per-file SHA-256 manifest has digest
`3e71b1a308a83cb4b55210d187eb43899cb2f30017b2cc1c5d2b013c4ed936ce`;
at least 711 files had arrived at this snapshot. PID `2853060` performs the first exact tree
comparison
after the tar stream exits, and PID `2853322` serially repeats the audit using committed script
`67aa62d`. A passing PE transfer receipt requires an identical file set, every file hash and total
byte count; neither audit uses a GPU.

The latest executable-code head is `f9f56a6`. Full CPU tests, Ruff, shell syntax checks and
`git diff --check` pass. Commits `094004f` and `8d67203` bind detector materialization to source
bank context and support the mixed 8/64-second bank contexts. Commit `fa8d02f` adds
glitch-head-only fixed-epoch/fixed-update scaling configurations and binds the AMPLFI runtime
handoff. Commit `811082c` makes head-only selection threshold-independent while retaining
validation-only threshold calibration. Commits `e9de51b`, `29c306f` and `ffb5630` add the real
AMPLFI smoke, resolve its deterministic CUDA incompatibility and permit safe use of one assigned
GPU on a shared host. Commits `fb4a781` and `0a70868` gate full score-blind background acquisition
on an exact pilot contract; `4623e8e` adds the disjoint high-range prefetch; `67aa62d` adds
cross-machine tree replay; `0531bed`/`cf877fc` add the chirp-frozen adapter plus its assigned-GPU
gate; and `3f886fb`/`f9f56a6` add identity-preserving pilot recovery with separate data-generator
and wrapper provenance. The draft review remains PR #2.

## 16:00 UTC execution and provenance closure

The primary replacement uniform arm is now at epoch 12/20. Overlap mean IoU has risen from
0.00548 to 0.17683, but clean-chirp IoU retention is only 0.49711 and no epoch is checkpoint
eligible. This is useful evidence that the model can learn the glitch mask while forgetting too
much of the chirp mask; it does not yet prove that more data are the limiting factor. At the latest
observed 5–6 minutes per epoch, the current arm should finish in approximately 45–60 minutes. A
complete two-arm decision is approximately 2–3 hours away; a passing branch that proceeds through
five seeds is approximately 10–14 GPU hours away. If both primary arms fail, the already queued
teacher-anchor fallback adds approximately 12–15 GPU hours.

The independent paired-PE corpus has been rebuilt against the exact new training-overlap identity.
The old receipt was correctly rejected because it bound SHA-256 `fbe29b...`, whereas the current
model uses `2292d2...`. The replacement receipt contains 210 validation-only physical mixtures,
passes the joint train/validation group audit, reads zero test rows and binds the current
source-safe train manifest. It is now the common source for the corrected mask and PE successors.

Three new commits close previously latent handoff failures:

- `fca187c` separates training-overlap, validation-overlap and clean-validation manifests in the
  paired-PE selector; one file can no longer satisfy two incompatible hashes.
- `925d544` adds a serial 3-event runtime proof followed by the predeclared 100-event/10,000
  bootstrap PE validation ladder, and teacher-anchor-specific fixed-epoch/fixed-update scaling.
- `62bc90b` makes the AMPLFI source-disjoint capacity extension consumable by stage-1 training. It
  freezes a hash-bound symbolic-link bank spanning base and extension exports without copying
  strain arrays, binds that bank report to checkpoint selection/model metadata, and rejects any
  test export.

The current full CPU suite, Ruff, every shell syntax check and `git diff --check` pass at
`62bc90b`. The exact checkout is deployed under
`/root/GW-YOLO-v2-amplfi-handoff-62bc90b`. Newly active or waiting successors are:

1. PID `217430`: AMPLFI extension receipt → no-copy bank → 100-epoch/20,000-update stage-1 model.
2. PID `217496`: teacher-anchor detector-set OOD plus two-control 250/500/1,000/full scaling.
3. PIDs `217855` and `217930`: primary/fallback 3-event → 100-event DINGO/AMPLFI PE ladders.
4. PIDs `218128` and `218247`: corrected primary/fallback six-arm mask and timing validation.
5. PIDs `218312` and `218384`: corrected primary/fallback numeric four-subset raw/mask successors.

PID `218070` is a malformed duplicate primary-mask waiter created during queue construction. Its
paths are empty, so after the upstream PID exits it will fail closed before opening data; corrected
PID `218128` is the authoritative replacement. It was left untouched under the no-process-
termination rule.

The first additional server has four RTX 5000 Ada GPUs, but all four are currently occupied at
70–100% utilization and were left untouched. The second server has approximately 2.4 TB free and
few CPU processes, but its container currently exposes no usable CUDA device: NVML reports
`Unknown Error` and PyTorch reports zero GPUs. It is retained as a possible CPU/data worker, not
counted as available GPU capacity. No external process was terminated.

## 15:00 UTC correction and live execution DAG

The first source-safe overlap arm is a retained negative result, not a completed model. Its
1,170/210 physical train/validation corpus passed the group audit, but all 20 epochs failed the
predeclared 0.95 clean-chirp retention gate. Clean retention ranged from 0.335 to 0.487 while
overlap mean IoU rose from 0.0049 to 0.2052. The failure was traced to the architecture boundary:
the pretrained checkpoint was fixed-channel, and averaging it into a detector-set model discarded
most clean performance before real-glitch training began.

The clean-only detector-set bootstrap completed all 30 epochs and passed its immutable
non-inferiority gate. The validation-selected epoch 27 has chirp IoU 0.0499701 versus 0.0342360
for the frozen fixed-channel teacher, a retention ratio of 1.45958 against the predeclared minimum
of 0.95. This is a successful architecture-transfer gate, not a five-seed or publication result.
PID `207452` has consequently entered the new one-seed/two-arm/five-seed source-safe chain using
checkpoint SHA-256 `aeeab93350129165d40afda394781c818e73c4d601ec0430cd98f521423ead2e`.
Its first uniform epoch has overlap mean IoU 0.00548 and clean-IoU retention 0.406, so catastrophic
forgetting remains a live risk even though the architecture transfer itself now passes. The
predeclared 20 epochs will complete without lowering the 0.95 gate. At the observed 147 updates per
9–10 minute epoch, the two one-seed arms require approximately 6–7 GPU hours and a full five-seed
completion approximately 18–24 GPU hours.

The detector-validation stream remains active as PID `188836`. The 121,376,902-byte H1 file is
complete and all eight V1 ranges for the same O3b source now total approximately 130 MB; the
download worker has restarted resumably and is assembling/retrying rather than starting over.
PIDs `190048` and `190936` remain the repair and physical PyCBC/LAL materialization successors.
The data volume has 11 GiB free; GPU 0 is occupied only by the child of PID `207452`, at about
1.14 GiB in the latest snapshot.

The corrected publication DAG is:

1. PID `207452`: passing clean detector-set checkpoint, now source-safe one/five-seed selection.
2. PID `207494`: four-subset numeric candidate baseline after both the five-seed and physical
   detector-data receipts.
3. PID `207534`: seven no-refit calibration perturbations against that frozen four-subset baseline.
4. PID `207944`: paired promoted/baseline candidate validation and, only on a pass, the authorized
   880-pair **H1/L1** continuous background.
5. PID `208257`: independent 210-scene six-arm mask validation and timing gate.
6. PID `208312`: complete raw/mask 880-pair background after the mask and model gates.
7. PID `208395`: detector-set OOD plus fixed-epoch/fixed-update 250/500/1,000 scaling curves.
8. PID `210564`: after the four-subset baseline and six-arm timing gates, rescore the numeric bank
   with retained probabilities and run paired raw/mask calibration on H1+L1, H1+V1, L1+V1 and
   H1+L1+V1.
9. PID `211556`: only after a complete negative primary chain, authorize a frozen teacher-anchored
   optimization fallback; if the primary chain passes, exit without GPU work.
10. PIDs `211951` and `212233`: conditional fallback H1/L1 candidate background, followed
    sequentially by its raw/mask 880-pair background.
11. PIDs `211997` and `212049`: conditional fallback four-subset numeric baseline and seven
    no-refit calibration scenarios.
12. PIDs `212111` and `212171`: conditional fallback six-arm mask/timing gate and numeric
    four-subset raw/mask successor.

Items 10–12 require a positive fallback five-seed summary and otherwise exit without opening
candidate scores. The fallback chain reuses the exact frozen train/validation overlap directories,
so it changes optimization only and does not create a new physical split or inflate sample counts.

The 880-pair H1/L1 background is no longer mislabeled as variable-detector evidence. A score-blind
capacity replay of the actual 215-block numeric detector bank found:

- 100/year cannot reach its 1.5× exposure target even with all 214 independent permutations;
- 1,000/year reaches the 90% zero-count target with 1.5× safety using 132 permutations and
  109,172 equivalent seconds;
- all four subsets are covered, with 26,033 H1+L1, 14,656 H1+V1, 13,864 L1+V1 and 13,630
  H1+L1+V1 eligible windows.

Therefore 1,000/year is frozen only for detector-subset and calibration-robustness validation. The
final search target remains 0.1/year on the large continuous background.

Commits `fc66856`, `94e5080` and `6ab34b0` repair the detector-set CLI, preserve observing-run
qualified GPS-block identities and define score-blind within-block physical-window ordinals for
arbitrarily centered validation windows. Commit `fbc605d` adds the clean bootstrap gate and the
numeric four-subset baseline. Commit `5b86dca` separates H1/L1 continuous evidence from
variable-detector evidence. Commit `6e59c0b` fixes an impossible train/validation overlap binding
in the mask gate. Commit `84b70f1` makes learned background deglitching replay frozen numeric
banks directly, including off-centre crops after source HDF eviction. Commit `1f31816` adds the
four-subset paired raw/mask successor, independently calibrates both timing arms, reuses the same
score-blind detector-set schedule, and explicitly forbids a final-search claim at the
1,000/year robustness operating point. The latest 56 targeted tests, Ruff, shell syntax checks and
`git diff --check` pass. Commit `0d966da` adds a validation-only teacher-chirp distillation
fallback at learning rate 1e-5, clean supervised weight 0.25 and distillation weight 4.0. Its
authorization requires either a completed negative source-safe receipt or a full 20-epoch history
with no retention-eligible checkpoint. The 0.95 retention gate is unchanged. A remote CPU smoke
test reproduces the hand-calculated `ln(2)` distillation loss.

## Earlier machine state (superseded where it conflicts with the 15:00 snapshot)

At 13:31 UTC the remote machine had 7.9 GiB free on the root volume and approximately 12 GiB free on
`/root/autodl-tmp`. A read-only storage audit found 8,877,873,374 bytes in 80 HDF5 files under the
failed, incomplete `gwosc-o4a-fresh-calibration-cache` acquisition. No process had an open handle
under that exact cache root, the failed acquisition had no accepted final receipt, and the frozen
independent endpoint and AMPLFI bank are stored elsewhere. An atomic intent and completion receipt
were written under `cache-evictions-20260723` before and after removing only that recoverable public
GWOSC cache. The source-safe overlap chain crossed its frozen 8 GiB guard at 13:30 UTC. It consumed
2,449 train plus 567 validation source rows and retained 1,170 train plus 210 validation physical
chirp/glitch overlaps under the frozen visibility and source-safety policy. The cross-split physical
group audit passes. The uniform seed-20260720 arm is now training on GPU 0; this is execution
progress, not a passing one-seed or five-seed result.

All 20 AMPLFI background shards (80 H1/L1 source pairs) have downloaded, exported and evicted their
verified source HDF cache. The result is deliberately not marked complete: the capacity gate
measured 195,408 train seconds against the predeclared 200,000-second minimum, a deficit of 4,592
seconds or 2.30%. Train GPS blocks (791) and both validation requirements (55,496 seconds and 228
blocks) pass. A small source-disjoint extension is required; rerunning the 80 pairs is not.
GPU 0 (RTX 4090) is now occupied by the uniform seed-20260720 overlap fine-tune, using approximately
1,170 MiB at the 13:45 UTC observation.

PID `156550` is the now-running source-safe overlap chain. It runs the one-seed arm comparison and
conditionally the five-seed gate. PID `168735` is a non-destructive successor: it retains OOD evidence regardless
of promotion and runs the fixed-epoch/fixed-update scaling matrix only if the five-seed gate passes.
PID `175740` is the expanded, purpose-disjoint 880-pair candidate successor and is the intended
publication path, but its background manifest and merge report do not yet exist; its empty runner
log confirms that it is waiting, not executing. It cannot be counted as candidate-background
evidence. Commit `0846855` therefore adds a replacement fail-closed chain. Remote PID `202405`
waits for the immutable source-safe receipt, retains either negative promotion as a completed null
result, and only after positive five-seed and paired candidate gates runs the promoted scorer,
authorized 880-pair stream and variable-detector calibration in order. PID `202620` then waits for
both that positive chain and the audited 400-injection detector-stratified corpus before executing
all seven no-refit calibration scenarios. PID `176490` is the variable
detector raw/mask common-FAR successor. PID `177139` binds its future result to deterministic
automatic-mask evidence. PID `184956` is the corrected full-100 DINGO/AMPLFI successor; unlike the
older stale queue, it points to the active five-seed path and waits until scaling/OOD is finished so
that it does not contend for GPU 0. The score-blind O3b metadata task has now frozen 32
source-disjoint pairs for both H1+V1 and L1+V1. PID `188836` is downloading the first two-pair
H1+V1 shard for numeric detector validation; the first 121,376,902-byte H1 source is complete and
the corresponding V1 ranged download is active. PID `190048` is a no-destructive-restart repair successor pinned
to the hardened eviction implementation. PID `189637` and final retry PID `190128` guard the
AMPLFI capacity extension behind detector acquisition. PID `190936` waits to perform real
PyCBC/LAL H1/L1/V1 signal projection after the detector data receipt exists. No existing
scientific process has been stopped or modified.
The O4b/GWTC-5 access-log path is absent.

The earlier three-person human-mask gate has been retired before O4b access. Reviewer disagreement
showed that a unique real-glitch pixel boundary was not a stable target. Existing annotations are
retained as historical diagnostics but are not merged, used for training or treated as ground
truth. The primary path now uses deterministic component-derived masks and functional fixed-FAR/PE
evidence.

## Workstream registry

| Workstream | Completion | Current evidence | Remaining hard gate | Estimate after prerequisites |
|---|---:|---|---|---:|
| Reproducible code, CLI, provenance and CPU tests | 99.9% | Stable CLI, numeric-bank scoring, no-copy AMPLFI bank, paired-PE ladder and fail-closed DAG; current full CPU suite passes | Final publication exports and result-driven fixes | 1–3 engineering days |
| Physical multi-Q/multi-IFO corpus | 98% | Four detector subsets and 400 physical validation injections pass; AMPLFI extension reaches 202,448 train and 56,520 validation seconds | Publication-scale continuous search stream and final DQ accounting | 2–10 days, network dependent |
| Automatic mask policy | 85% execution / 100% protocol | 100 validation scenes rematerialized; source components and masks replay exactly | Bind passing raw/mask continuous endpoint | 1–3 days, upstream dependent |
| Five-seed model selection | 50% execution / 100% protocol | Full-model and teacher-anchor arms are retained negatives; head-only is 14/20 with retention 1.000 and falling validation loss; a validation-loss successor is conditional on failure | Positive one-seed arm, then at least 4/5 passing seeds | 2–6 GPU hours for one-seed decision; 10–18 additional GPU hours if promoted |
| Fixed-epoch/fixed-update data scaling | 18% execution / 100% protocol | Head-only 250/500/1k/full and hard-subset controls are hash-bound and queued behind five-seed promotion | Material improvement on both controls before scaling farther | 3–6 single-GPU days; 1–2 days after verified auxiliary capacity |
| Continuous-background FAR/IFAR/`<VT>` | 14% execution / 100% locked-reduction infrastructure | Authorized H1/L1 880-pair chain is queued separately from four-subset evidence | Positive model gate, 880-pair acquisition/scoring; then ≥23.02585093 post-DQ equivalent years and ≥3,000 DQ-eligible injections | 3–14 days, strongly network dependent |
| Raw versus mask search | 22% execution / 100% infrastructure | Corrected 210-overlap receipt passes against the active train hash; primary/fallback mask and numeric four-subset successors are queued | Same background/injections, frozen thresholds, clean non-inferiority and positive paired gain | 1–4 days after model freeze |
| Detector-set/O4 OOD transfer | 12% execution / 98% infrastructure | Abstention/OOD protocol is frozen | Legitimate labeled/audited O4 sources, ≥500 locked rows | 1–4 days |
| Calibration and missing-IFO robustness | 55% execution / 100% infrastructure | Four-subset stream and 400-injection physical materialization pass; baseline and seven no-refit scenarios are queued | Positive five-seed checkpoint, baseline and perturbation replay | 4–18 hours after model promotion |
| DINGO/AMPLFI paired PE | 40% evidence / 100% protocol | Exact 210-event input receipt and capacity-passing 1,054-file bank exist; real smoke passes and corrected stage-1 is active on GPU 3 | Passing AMPLFI stage-1 metadata, then ≥100 identical BBH over ≥25 GPS blocks | 2–4 hours for stage-1 plus 1–4 days for paired posteriors |
| Validation publication ledger | 30% (3/10 verified) | Source-safe corpus, independent endpoint and unopened corpus replay pass | Seven immutable evidence items | 2–10 days; detector-stratified data now explicit |
| Locked O4b/GWTC-5 final ledger | 0% (0/9) | Correctly unopened | Validation 10/10, then one-time execution | 1–3 weeks after unlock |
| Paper-ready evidence and manuscript | ~21% | Methods, governance, physical validation data and PE capacity are strong; the primary positive result remains absent | Positive or retained null results, uncertainty, ablations, tables | 3–8 weeks |

These estimates are ranges, not guarantees. GWOSC throughput, DQ attrition, backend runtime and
negative promotion gates can extend them. A failed scientific gate is retained
as a result; it is not silently bypassed to preserve the schedule.

## New locked streaming and PE retention work

The streaming shard contract now publishes every raw and mask candidate instance without
threshold filtering, retains explicit null outcomes, validates OOD numeric sources, and merges all
shards into the predeclared suite inputs. Post-DQ injection weights are copied into both search arms
from one shared physical denominator.

A critical PE source-lifetime gap is now closed before source HDF5 eviction. The pre-access plan
binds the passing validation PE promotion, the exact common BBH prior, H1/L1, all three PE
conditions, the selection seed and a score-independent GPS-block-first hash order. A read-only audit
of the real 4,860-row locked inventory found 492 prior/IFO-compatible BBH injections in 492 distinct
GPS blocks. The plan freezes the first 300 as a DQ reserve for a final minimum of 100. There is no
post-access replacement.

For every selected injection that passes DQ, shard publication must retain complete clean,
contaminated and mask-conditioned numeric strain arrays with matching injection, waveform,
GPS-block, IFO and SHA-256 identities. Missing one triplet member, a non-finite tensor, a changed
hash or an escaped path prevents publication and explicitly forbids source eviction. The final
all-shard merger requires the retained set to equal the exact intersection of the frozen 300-event
pool and post-DQ eligibility.

The full CPU suite, Ruff, all shell syntax checks and `git diff --check` pass on the current
detector-validation streaming implementation.

## Validation ledger and detector-stratified data audit

A direct replay with the current official validation protocol now passes **3/10** gates with zero
failures: source-safe corpus, independent validation endpoint and locked corpus unopened. The prior
2/10 status omitted the already valid unopened-corpus contract. Seven evidence items are still
pending; no pending item is counted as passed.

Commit `b195836` closes a critical ledger loophole. The ledger now rejects the old H1/L1
`gps_block_permutation` evidence and requires the H1/L1/V1
`variable_detector_set_block_permutation`, multiway dependence bootstrap and expanded-background
lineage. The raw/mask gate likewise requires detector-set dependence audits. Positive and negative
unit tests prove that the new artifacts pass and the old artifacts fail.

Commits `bb8453b`, `40a30f0` and `4d2d603` extend the same rule to calibration perturbations. All
seven physical time-domain perturbations must be evaluated without threshold refitting on
H1+L1, H1+V1, L1+V1 and H1+L1+V1. Each exact detector stratum requires at least 25 injections.
The runner checks this before GPU work, and the aggregate and publication ledger repeat the gate.

The physical count audit explains why this is a real data task rather than a schema change:

- the current independent 3,000-injection endpoint is 3,000 H1+L1 and zero in the other three
  detector subsets;
- after enforcing one canonical row per independent network GPS block, the source-safe validation
  bank has 102 H1+L1, 14 H1+V1, 4 L1+V1 and 95 H1+L1+V1 blocks;
- the corresponding strict training counts are 560 H1+L1, 82 H1+V1, 35 L1+V1 and 425
  H1+L1+V1 blocks.

Therefore the next corpus expansion must add at least 11 H1+V1 and 21 L1+V1 independent,
validation-only physical contexts, and should target substantially more than the hard floor. The V1
channel must contain an actual antenna-projected waveform, not a zero-filled validity placeholder.
No train-row reassignment is allowed.

The new acquisition chain consumes only source-file/GPS-disjoint O3b plans and processes bounded
shards in this exact order: verified GWOSC download, catalog-event/DQ filtering, frozen
`hash_threshold_v1` assignment with zero test fraction, one numeric context per validation GPS
block, full artifact replay, and recoverable HDF eviction. Each shard receives an immutable receipt.
Only a cross-shard merge with zero repeated window/GPS identities and all four detector subsets at
or above 25 blocks can freeze the equal-count physical injection recipes. Candidate scores and test
strain are never opened by this path.

Commit `c5f5477` implements that shard seal/merge chain and is deployed at exact full commit
`c5f547732386a4f29b23dbff067bdef72857a1d8`. Commit `04b3072` adds the independent AMPLFI
capacity extension. It retains the original background seed and capacity policy, excludes both the
80-pair parent and its inherited 880-pair reservation, downloads at most eight new pairs in
two-pair shards, and stops at the first capacity pass after verified export and source eviction.
Both commits pass the full CPU suite, Ruff, all shell syntax checks and `git diff --check`.

Commit `14630d6` closes the DQ-zero-yield cleanup edge case. Its atomic eviction intent covers every
verified file in the downloaded shard, including a pair that contributes no validation block, and
can safely finish a partially completed deletion after interruption. Commit `b24ecd1` adds the
physical successor: external waveform equivalence, PyCBC/LAL detector projection, empirical
per-IFO SNR, geometric arrival annotation, and a final tensor-level audit requiring nonzero
analysis-window signal in every declared H1/L1/V1 channel. The expected frozen corpus is 400
injections, 100 per exact detector subset, with 300 containing a real V1 projection.

## Variable-detector locked search closure

Commit `b07787a` closes a previously hidden locked-evaluation failure: the suite declared H1+L1,
H1+V1, L1+V1 and H1+L1+V1, while the old reducer could only construct H1/L1 pairs and did not
create the four predeclared final search inputs. Injection ranking now evaluates every eligible
detector subset, applies every pairwise light-travel limit plus the frozen empirical timing
allowance, retains explicit missing-IFO semantics and prefers a larger coherent network only when
the ranking score ties.

The pre-access streaming plan now freezes a score-blind independent-offset schedule with H1 at
zero, L1 shifted forward and V1 shifted backward. Raw and mask arms replay the same schedule,
physical denominator and injection trigger set. Detector duty cycle is evaluated at each shifted
source window; all eligible subset channels are clustered jointly, so a single event cannot be
counted four times. The reduction fails closed unless every detector subset and source family is
covered and the background, injection, OOD and paired-PE minima all pass.

A read-only replay against the real 4,860-row O4b availability manifest selected 65 effective
offsets through slide index 65. It predicts 34.6749654 equivalent years, just above the
predeclared 1.5× safety target of 34.5387764 years. The schedule contains 177,421 H1+L1,
183,564 H1+V1, 186,970 L1+V1 and 140,401 H1+L1+V1 eligible shifted block instances. This is
pre-access capacity evidence only: the final reducer still requires at least 23.02585093 years
after DQ attrition and will not replace rejected blocks.

## Variable-detector validation identity closure

Commits `252fd06` and `124147f` close the validation/test candidate-identity mismatch. Validation
can now freeze score-blind independent circular block permutations with H1 fixed, L1 shifted
forward and V1 shifted backward, enumerate H1+L1, H1+V1, L1+V1 and H1+L1+V1, and cluster those
channels jointly. The threshold calibration replays the exact schedule and reconstructs union
exposure from detector availability rather than multiplying pair channels.

The new dependence audit assigns each exposure cell to as many as three physical source GPS
blocks plus its permutation offset. It reports a multiway source-block×offset pigeonhole bootstrap,
leave-one-block and leave-one-offset sensitivity, effective block/offset counts and exposure
dominance. A hand-calculated test reconstructs 40 seconds, five exceedances and
`FAR = 5 × 31557600 / 40`, and the full CPU suite passes.

The remote successor at commit `124147f1867ac217c7155addf7d6b384f65efb50` is deployed in a new
directory and queued as PID `174868`. A read-only audit found that the only completed legacy
candidate pipeline has 245.776 ms empirical timing uncertainty, so it is correctly rejected by the
frozen 10 ms network gate. The successor will only run on the pending five-seed-selected promoted
pipeline; it does not silently reuse the legacy timing result.

## Deterministic automatic-mask replay

Commit `290ef528a5871a3558b5f0b331aae960ec1e18b9` removed an unnecessary dependency on evicted
background strain when replaying a materialized injection signal. A fresh remote checkout then
rematerialized 100 validation overlaps from the original Gravity Spy numeric strain and isolated
injection components. The frozen set contains 100 unique injections, waveforms and glitches over
65 GPS blocks and 19 glitch labels; H1/L1 counts are 71/29.

The immutable audit
`/root/GW-YOLO-v2-artifacts/automatic-mask-publication-290ef52/automatic_mask_policy_audit.json`
passes with source-artifact hashes, component hashes, detector availability and the exact
`mixture = glitch + signal` identity replayed. Every glitch pseudo-mask is non-empty. Thirty-eight
chirp masks are empty because the frozen per-IFO SNR visibility policy suppresses sub-threshold
targets; they are retained as negative/null cases. The audit explicitly records
`human_ground_truth_claimed=false`, `pixel_accuracy_claim_allowed=false`, `test_rows_read=0` and
`scientific_claim_allowed=false`. It becomes validation-ledger evidence only after PID `168401`
binds it to a passing fixed-FAR raw/mask endpoint.

## Next execution order

1. Complete the now-running one-seed/two-arm/five-seed chain from the passing clean detector-set
   bootstrap. Retain a negative result if either promotion or stability fails; only then permit the
   frozen teacher-anchored optimization fallback.
2. Finish the resumable H1+V1/L1+V1 stream, close the AMPLFI 4,592-second deficit, and materialize
   100 real antenna-projected injections per exact detector subset.
3. Freeze the 1,000/year four-subset numeric robustness baseline and run all seven no-refit
   calibration scenarios. Never use this operating point as the final search FAR.
4. If paired candidate validation passes, acquire and score the authorized 880-pair H1/L1
   background at 0.1/year. Keep this evidence separate from four-subset calibration.
5. Run the independent 210-scene mask/timing gate and the paired raw/mask 880-pair background;
   execute the already queued numeric four-subset mask-conditioned successor on the same frozen
   schedule.
6. Complete detector-set OOD and both fixed-control data-scaling curves before authorizing any
   50k/200k same-distribution expansion.
7. Produce the validation PE portfolio, OOD, calibration and continuous-background artifacts and
   replay the validation ledger to 10/10.
8. Freeze the newest exact commit and the 300-event PE reserve. Only then create the one-time O4b
   access record.
9. Execute streaming raw/mask/OOD/PE retention, reduce FAR/IFAR/`<VT>`, run paired DINGO/AMPLFI
   backends, and finalize the nine locked endpoints.
10. Export positive, negative and null results into the paper registry; complete ablations,
   uncertainty tables and the PRD/JCAP/ApJS manuscript.
